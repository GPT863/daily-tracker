# 健康日记 V1.0 移动端评审报告

**评审日期：** 2026-04-04
**评审范围：** 移动端 UI 布局、交互设计、业务流程、业务逻辑、视觉效果
**评审环境：** iPhone 14 模拟尺寸（390×844，3x DPR）

---

## 一、UI 布局

### 问题

**1. 首页 Header 占屏比过大、信息密度低**
绿色渐变 Header 约占屏幕高度的 11%，但只放了一个日期选择器，视觉分量与信息价值严重不匹配。日期选择是辅助导航，不应成为视觉焦点。

**2. 「记录」页标题随 Tab 切换变化（活动管理 / 测量管理 / 症状管理）**
「管理」语义过重，暗示增删改查全数据，但页面实际只展示当天记录，与用户场景不符，应改为「今日活动」「健康测量」「症状记录」。

**3. 「我的」页无用户身份感**
直接是功能列表，没有用户头像、姓名、健康摘要的 Profile Header，显得冷漠，且无法直观知道当前是谁的数据。

**4. 「今日状态记录」全屏按钮遮挡输入区**
右上角圆形全屏按钮叠在 textarea 上，遮挡右上角约 44×44px 的可输入区域，点击容易误触。

**5. 记录卡片按钮（编辑 / 删除 / 标记完成）常驻显示，挤压内容空间**
每张卡片底部固定排列 2~3 个 pill 按钮，在提醒列表里每张卡片有 3 个按钮占满整行，视觉噪音大，内容区域被压缩。

---

## 二、交互设计

### 问题

**1. 底部「+记录」按钮语义与行为不符**
中间突出的绿色+号按钮，图标语义是「新增」，但点击后跳转的是「记录管理列表页」而非「新增记录弹窗」。用户期望点+直接进入填写，实际需再点一次 Header 里的「+新增」——多了一步无意义跳转。

**2. 提醒弹窗打断任何当前操作，且无法真正关闭**
到点提醒弹窗会覆盖在任何页面/弹窗之上，包括正在填写的「添加活动」表单。弹窗只有「稍后处理」「延后提醒」「标记完成」三个操作，没有「X 关闭」，用户一旦被打断无法回到原来的操作。

**3. 日期选择器只有左右箭头，没有日历弹窗视觉提示**
移动端用户习惯点击日期直接弹出日历选择，仅靠左右箭头逐天翻阅，跨周跨月效率极低。点击日期中间区域虽然有日历，但视觉上完全没有提示这是可点击的。

**4. 两个核心表单（添加活动 vs 添加提醒）风格割裂**
- 添加活动：浅绿背景 + 快速模板区 + bottom sheet
- 添加提醒：纯白背景 + 无模板 + 同样是 bottom sheet

同是核心录入路径，视觉语言、字段标签风格不一致。

**5. 缺少移动端手势操作**
无左划删除、无长按编辑，与 iOS/Android 原生手势完全脱节，所有操作依赖常驻按钮。

**6. 添加活动默认「开始 = 结束时间」**
大量活动（睡眠、运动）有时长，默认两者相等时时长计算为 0 分钟，用户必须手动修改结束时间，容易遗忘，导致时间线显示「0 分钟」的脏数据。

---

## 三、业务流程

### 问题

**1. 新增记录的入口分散、路径不统一**
- 首页：无快速入口
- 底部 +：进列表，再点 Header +新增
- 「今日数据」→「去测量」：进测量弹窗
- 「时间线」→「去添加」：进活动弹窗

同一个「记录」动作，在不同地方触发走的是不同路径，用户心智负担重。

**2. 提醒 → 执行 → 记录 闭环断裂**
提醒弹窗「标记完成」后，直接关闭。健康应用的核心价值是：**提醒我做了 → 引导我记录下来**。完成提醒后应提示「是否同时记录一条活动？」，否则提醒与记录是两条完全断开的业务线。

**3. 「今日状态记录」与「时间线」关系不透明**
用户自然会以为保存的日记会出现在时间线里，但实际是独立的每日笔记，时间线只展示活动/测量/症状。首页这两个模块紧挨着排列，容易让用户误解数据会合并显示。

**4. 家庭成员视角切换路径过深**
查看某个家庭成员的健康数据：我的 → 家庭成员 → 选成员 → 查看档案。这是静态档案，不是「今日数据」。家庭成员的当日记录（如帮父母测了血压）无法以其身份录入，记录始终归属当前用户——家庭健康管理的核心需求没有真正实现。

**5. 「历史数据」入口过于隐蔽**
这是个重要功能，但入口是首页「今日数据」卡片右上角的一个小圆角 tag，样式与普通标签无异，容易被忽视。

---

## 四、业务逻辑

### 问题

**1. 测量数值无正常范围提示**
血压 116/74、血糖 6.2、尿酸 349 umol/L——对普通用户这些数字没有意义，卡片上没有参考范围、无颜色预警（偏高 / 正常 / 偏低）。这是健康应用的核心价值缺失。

**2. 「今日提醒」与「全部提醒」数据重叠**
今日提醒在「全部提醒」Tab 里也完整显示，用户切换时会看到重复的条目，显得冗余。「全部提醒」应该是按时间/类型分组的完整管理视图，而不是「今日」的超集。

**3. 重复提醒没有「本次跳过」逻辑**
每日重复的提醒（如早餐、服药）一旦「标记完成」即计为完成，第二天会重新出现——这是对的。但如果今天想跳过（不是延后，就是今天不做），没有「跳过本次」选项，只能「稍后处理」无限推迟。

**4. 症状记录没有严重程度字段**
记录「肩颈紧绷」「咽喉偏干」后，无法量化症状强度（如 1-10 评分），后续趋势分析就缺乏关键维度。

---

## 五、视觉效果

### 亮点
- 翡翠绿设计系统整体协调，色感舒适
- 今日健康数据卡片的彩色左竖条 + emoji 组合识别度高
- 字体层次清晰（标题粗体绿色数值 + 灰色辅助文字）
- 底部导航的活跃状态（绿色+号）设计清晰

### 问题

**1. 空状态视觉质量明显低于非空状态**
「记录」页空状态仅一行灰色小字，无插图、无 CTA 颜色引导，与有数据时的丰富视觉形成强烈落差。

**2. 提醒卡片中图片占比过大**
「记得服药」卡片里的提醒图片高度约 120px，在列表视图中侵占性太强，3 张卡片就让页面显得头重脚轻。建议列表视图缩略图化（48px），详情弹窗才展示大图。

**3. 「症状」时间线卡片缺少左侧颜色竖条**
活动卡片有颜色竖条（橙/绿/紫等），测量卡片也有（青绿），但症状卡片没有——视觉系统不完整，三个 Tab 的设计语言应统一。

**4. 「添加提醒」表单是纯白底**
与「添加活动」的浅绿背景形成明显差异，两个同等重要的核心表单视觉权重不一致。

**5. Header 底部边缘过渡生硬**
首页绿色 Header 与白色卡片区之间没有过渡处理（无渐变、无阴影、无圆角延伸），截断感强。

---

## 优先级汇总

| 优先级 | 问题 | 分类 |
|--------|------|------|
| 🔴 高 | 提醒弹窗无法关闭 / 打断其他操作 | 交互 |
| 🔴 高 | 底部+号语义与行为不符 | 交互 |
| 🔴 高 | 新增记录入口分散不统一 | 业务流程 |
| 🔴 高 | 测量数值无参考范围 / 无预警 | 业务逻辑 |
| 🟠 中 | 提醒→执行→记录闭环断裂 | 业务流程 |
| 🟠 中 | 家庭成员视角无法真正切换记录 | 业务逻辑 |
| 🟠 中 | 今日状态记录与时间线关系不透明 | 业务流程 |
| 🟠 中 | 重复提醒缺少「跳过本次」逻辑 | 业务逻辑 |
| 🟠 中 | 提醒卡片图片占比过大 | 视觉 |
| 🟡 低 | 空状态视觉单薄 | 视觉 |
| 🟡 低 | 症状卡片缺少颜色竖条 | 视觉 |
| 🟡 低 | 添加提醒与添加活动表单风格不统一 | 视觉 / 交互 |
| 🟡 低 | 「我的」页缺少用户身份 Profile Header | 布局 |
| 🟡 低 | 添加活动默认开始=结束时间 | 交互 |
