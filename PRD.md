# 产品需求文档（PRD）

## 1. 文档信息

- 产品名称：健康生活记录器 / 日常活动记录器
- 文档版本：V2.0（原型阶段完善版）
- 文档日期：2026-03-10
- 产品形态：移动优先的单页 Web 应用（PWA）
- 当前实现状态：原型阶段，基础功能已实现
- 版本变更：基于V1.0评审反馈，完善产品功能设计

## 2. 产品概述

### 2.1 产品背景

用户在日常健康管理中，常常需要记录饮食、用药、运动和身体感受，同时希望通过简单提醒维持规律作息。现有通用笔记工具记录成本高、结构不统一，健康类 App 又常伴随登录、云同步、广告或复杂流程，不适合轻量自用。

本产品定位为一个轻量、免登录、可本地保存的健康生活记录工具，帮助用户以最低成本记录每日活动并查看当日健康行为概览。

### 2.2 产品目标

- 帮助用户快速记录日常健康相关活动
- 帮助用户形成规律的饮食、用药、运动习惯
- 提供按日查看和基础统计能力，便于回顾
- 提供提醒与导出能力，支持持续使用和数据备份

### 2.3 产品定位

一个面向个人用户的轻量健康记录工具，强调：

- 无需注册登录
- 数据本地保存
- 打开即用
- 以手机端使用体验为主

## 3. 目标用户

### 3.1 用户类型

- 需要记录饮食、用药、运动的个人用户
- 有基础健康管理需求的中老年用户
- 需要观察身体反应与生活习惯关系的用户
- 需要低门槛提醒功能的用户

### 3.2 用户特征

- 更关注简单易用，而非复杂功能
- 更关注隐私，不希望上传敏感健康数据
- 使用设备以手机浏览器为主，也可能在电脑端访问
- 能接受手动导出备份，不强依赖实时同步

## 4. 核心使用场景

### 4.1 记录用餐

用户在早餐、午餐、晚餐或加餐后，记录时间、内容和感受，用于回顾饮食规律和身体反应。

### 4.2 记录用药

用户在服药后，记录服药时间、药物名称和服药后的身体反应，用于减少漏服并积累个人健康数据。

### 4.3 记录运动

用户在运动后，记录运动内容、时间、时长和感受，用于查看当天运动次数和总时长。

### 4.4 记录睡眠

用户可记录入睡、午休、补觉等睡眠相关活动，用于回顾睡眠时间与身体状态变化。

### 4.5 设置提醒

用户提前创建吃饭、吃药、运动等提醒，在指定时间收到浏览器通知或应用内提醒。

### 4.6 查看当天概况

用户打开首页即可看到当日用餐次数、用药次数、运动次数和运动时长，并浏览时间线。

### 4.7 导出数据备份

用户定期将记录导出为 JSON 或 CSV，用于本地备份或使用 Excel 分析。

## 5. 产品范围

### 5.1 V2.0 功能范围（原型完善阶段）

#### 5.1.1 核心功能（已完成）

- ✅ 活动记录（用餐、用药、运动、睡眠、工作、其他）
- ✅ 活动编辑与删除
- ✅ 图片上传（拍照/相册）与压缩处理
- ✅ 按天浏览历史记录
- ✅ 当日统计展示
- ✅ 提醒创建、查看、完成、删除
- ✅ 每日重复提醒
- ✅ JSON / CSV 导出（按日期）
- ✅ LocalStorage 本地存储
- ✅ 移动端适配
- ✅ 健康数据记录（血压、心率、血糖、血脂、尿酸、其他）
- ✅ 个人信息管理
- ✅ 长辈模式

#### 5.1.2 需要完善的功能（P0优先级）

**数据持久化增强**
- 🔴 数据导入功能（JSON文件恢复）
- 🔴 迁移至 IndexedDB 存储（解决容量限制）
- 🔴 自动备份提醒机制
- 🔴 存储容量监控与预警

**提醒功能完善**
- 🔴 提醒编辑功能
- 🔴 提醒搜索与筛选
- 🔴 提醒延后时间自定义（当前仅10/15/30分钟）
- 🔴 提醒历史记录

**数据验证**
- 🔴 健康数据范围校验（如血压范围、血糖合理值）
- 🔴 时间合理性检查（防止未来时间、过大偏差）
- 🔴 重复提交防护

**PWA 离线能力**
- 🔴 Service Worker 注册与离线缓存
- 🔴 离线访问支持
- 🔴 后台同步提醒（依赖Service Worker）

#### 5.1.3 新增功能（P1优先级）

**数据统计增强**
- 🟡 周/月统计视图
- 🟡 健康数据趋势图表
- 🟡 数据对比分析（周环比、月环比）
- 🟡 运动时长统计优化（小时+分钟显示）

**搜索与筛选**
- 🟡 历史记录全文搜索
- 🟡 按活动类型筛选
- 🟡 按日期范围查询
- 🟡 按关键词搜索提醒

**导出功能增强**
- 🟡 全量数据导出（所有历史数据）
- 🟡 导出范围选择（日期范围、数据类型）
- 🟡 提醒数据导出
- 🟡 导出历史记录

**用户体验优化**
- 🟡 图片预览放大功能
- 🟡 暗色模式支持
- 🟡 首次使用引导教程
- 🟡 数据为空时的引导提示
- 🟡 加载状态提示

#### 5.1.4 次要功能（P2优先级）

**数据管理**
- 🟢 数据标签系统（自定义标签）
- 🟢 数据分类管理
- 🟢 批量操作（批量删除、批量导出）

**分享与协作**
- 🟢 分享活动记录
- 🟢 导出PDF健康报告
- 🟢 打印友好格式

**高级统计**
- 🟢 自定义统计周期
- 🟢 数据可视化图表（柱状图、折线图、饼图）
- 🟢 健康数据异常预警

**辅助功能**
- 🟢 语音输入支持
- 🟢 数据模板（常用活动快速记录）
- 🟢 数据备注与标记

### 5.2 不在产品范围

- 用户注册登录系统
- 云端账号体系
- 实时多设备同步（可选云备份除外）
- 服务端存储（保持纯前端）
- 医疗诊断建议
- 在线问诊功能
- 社交功能
- 第三方数据源接入（如智能手环）

## 6. 信息架构

### 6.1 页面结构

当前产品为单页面结构，主要由以下区域构成：

1. 顶部日期区
2. 当日统计卡片区
3. 今日提醒区
4. 活动时间线区
5. 底部快捷操作区
6. 活动录入弹窗
7. 提醒管理弹窗
8. 导出弹窗

### 6.2 导航方式

- 通过顶部左右按钮切换日期
- 通过底部悬浮按钮进入新增活动与提醒管理
- 通过弹窗内标签切换不同提醒视图

## 7. 功能需求

### 7.1 活动记录模块

#### 7.1.1 新增活动

用户点击底部"+"按钮后，打开活动录入弹窗，填写以下字段：

- **时间**：必填，精确到分钟
  - 默认值：当前时间
  - 约束：允许调整但不允许未来时间，过去时间给予提示
  - V2.0新增：时间合理性校验，偏差过大时弹出确认提示
- **类型**：必填，从预设类型中选择
- **内容**：必填，文本输入，支持多行
- **感受**：选填，文本输入
- **时长**：选填，单位为分钟，数字输入
  - V2.0新增：自动计算建议时长（基于历史数据）
- **图片**：选填，支持拍照或从相册选择
  - V2.0新增：图片自动压缩（最大1600px，质量0.82）
  - V2.0新增：图片预览与删除功能

活动类型包括：

- 用餐 🍽️
- 用药 💊
- 运动 🏃
- 睡眠 😴
- 工作 💼
- 其他 📌

#### 7.1.2 编辑活动

用户可对已有活动进行编辑，修改原有字段并保存。

- V2.0新增：编辑时保留原有图片，可修改或删除
- V2.0新增：编辑历史记录（可选功能）

#### 7.1.3 删除活动

用户可删除已有活动，删除前需进行二次确认。

- V2.0新增：支持批量删除（多选模式）
- V2.0新增：删除后支持撤销（30秒内）

#### 7.1.4 时间线展示

系统按时间顺序展示当前日期下的全部活动记录，每条记录显示：

- 时间
- 类型图标
- 内容
- 图片（如有）
- 感受
- 时长
- 编辑 / 删除操作

**V2.0增强功能：**
- 支持正序/倒序切换
- 图片点击放大预览
- 长按快速编辑/删除
- 时间线虚拟滚动（数据量大时）

#### 7.1.5 空状态

当某日无活动记录时，页面应展示空状态提示，引导用户新增记录。

- V2.0新增：显示友好的引导文案和快捷操作按钮
- V2.0新增：提供常见活动快速添加建议

#### 7.1.6 搜索与筛选（V2.0新增）

- **全文搜索**：搜索活动内容、感受
- **类型筛选**：按活动类型过滤
- **日期范围**：选择起止日期查询
- **高级搜索**：组合条件（类型+日期+关键词）

### 7.2 日期浏览模块

#### 7.2.1 按日切换

用户可通过顶部左右按钮切换前一天或后一天，查看对应日期的活动记录。

- V2.0新增：日期选择器快速跳转到指定日期
- V2.0新增：日历视图显示有记录的日期标记

#### 7.2.2 日期展示

页面顶部显示当前所查看的完整日期信息，包括年、月、日和星期。

- V2.0新增：快速返回今天按钮
- V2.0新增：显示当天活动数量统计

#### 7.2.3 快速导航（V2.0新增）

- 昨天/今天/明天快捷切换
- 历史记录日历视图
- 有记录日期高亮显示

### 7.3 统计模块

#### 7.3.1 当日统计

系统需基于当前日期下的活动记录，自动统计并展示以下指标：

- 用餐次数
- 用药次数
- 运动次数
- 运动总时长（小时+分钟）

统计结果需在用户新增、编辑、删除活动后实时更新。

#### 7.3.2 周/月统计（V2.0新增）

**周统计：**
- 本周各类型活动总数
- 本周运动时长合计
- 本周与上周对比（环比）
- 周平均活动次数

**月统计：**
- 本月各类型活动总数
- 本月运动时长合计
- 本月健康数据趋势
- 月平均活动次数

#### 7.3.3 数据可视化（V2.0新增）

**趋势图表：**
- 活动次数趋势（折线图）
- 运动时长趋势（柱状图）
- 健康数据变化曲线（血压、血糖等）

**分布图表：**
- 活动类型分布（饼图）
- 用餐时间分布（热力图）
- 运动时长分布（直方图）

#### 7.3.4 数据对比（V2.0新增）

- 周环比对比
- 月环比对比
- 自定义时间段对比
- 健康数据达标率

### 7.4 提醒模块

#### 7.4.1 新增提醒

用户可新增提醒，字段包括：

- **提醒内容**：必填
- **类型**：必填，从预设类型选择
- **日期**：必填，支持选择未来任意日期
- **时间**：必填，精确到分钟
- **是否每日重复**：选填，勾选后自动创建每日提醒
- **备注**：选填
- **图片**：选填（V2.0新增）

提醒类型包括：

- 用餐 🍽️
- 用药 💊
- 运动 🏃
- 睡眠 😴
- 其他 📌

#### 7.4.2 提醒视图

提醒管理弹窗内应提供以下标签页：

1. **今日提醒**：显示当天未完成提醒
2. **即将到来**：显示当前时间之后最近 10 条未完成提醒，按时间升序排序
3. **全部提醒**：显示所有提醒，包括已完成和已过期
4. **添加提醒**：用于录入新提醒

**V2.0新增视图：**
5. **提醒历史**：已完成的提醒记录
6. **搜索提醒**：搜索和筛选提醒

#### 7.4.3 首页提醒摘要

首页展示"今日提醒"摘要区，显示当天最多 3 条未完成提醒。

- V2.0新增：点击提醒卡片直接跳转到详情
- V2.0新增：提醒缩略图展示（如有图片）

#### 7.4.4 完成提醒

用户可将提醒标记为完成。

若提醒为每日重复提醒，则在用户完成当前提醒后，系统自动创建下一日同时间的新提醒。

- V2.0新增：完成时添加备注
- V2.0新增：完成时间记录

#### 7.4.5 编辑提醒（V2.0新增）

用户可编辑已创建的提醒，可修改以下字段：

- 提醒内容
- 类型
- 日期和时间
- 是否每日重复
- 备注
- 图片

**编辑规则：**
- 已完成的提醒不允许编辑
- 已过期的提醒编辑后重新计算提醒时间

#### 7.4.6 删除提醒

用户可删除提醒，删除前需进行二次确认。

- V2.0新增：支持批量删除
- V2.0新增：删除后支持撤销（30秒内）

#### 7.4.7 通知触发

当系统检测到当前时间与提醒时间一致时：

- 若用户已授权浏览器通知权限，则触发系统通知
- 若未授权，则以应用内 Toast 方式提示

**V2.0增强：**
- 到点提醒弹窗（显示提醒详情、图片、备注）
- 提醒延后功能（10/15/30分钟，或自定义）
- Service Worker后台推送（需PWA完善）

#### 7.4.8 提醒延后（V2.0完善）

- 预设延后时间：10分钟、15分钟、30分钟
- 自定义延后时间：用户输入具体分钟数
- 延后次数限制：单条提醒最多延后3次
- 延后记录：显示已延后次数和总时长

#### 7.4.9 提醒搜索与筛选（V2.0新增）

- **搜索提醒**：按内容、备注搜索
- **筛选提醒**：按类型、状态（已完成/未完成）、日期范围筛选
- **排序方式**：按时间、按类型、按状态排序

### 7.5 健康数据模块（V2.0新增）

#### 7.5.1 记录健康数据

用户可记录健康指标，字段包括：

- **时间**：必填，默认当前时间
- **指标类型**：必填
  - 血压（mmHg）
  - 心率（bpm）
  - 血糖（mmol/L）
  - 血脂（mmol/L）
  - 尿酸（umol/L）
  - 其他指标
- **数值**：必填
- **单位**：自动填充，可修改
- **备注**：选填（如：饭后测量、运动后测量）
- **图片**：选填（检测报告、仪器屏幕、试纸结果）

#### 7.5.2 数据校验

**血压校验：**
- 格式：收缩压/舒张压（如120/80）
- 收缩压范围：60-250 mmHg
- 舒张压范围：40-150 mmHg
- 异常值提示并要求确认

**心率校验：**
- 范围：30-220 bpm
- 异常值提示

**血糖校验：**
- 范围：1.0-30.0 mmol/L
- 异常值提示

**血脂/尿酸：**
- 参考医学正常范围
- 超出范围给予提示

#### 7.5.3 健康数据展示

**首页健康数据卡片：**
- 按类型显示最新记录
- 显示数值、单位、测量时间
- 如有图片显示缩略图

**时间线展示：**
- 与活动记录混合展示
- 健康数据特殊标识（🩺图标）
- 显示数值和备注

#### 7.5.4 健康趋势分析（V2.0新增）

- 各指标趋势图表
- 异常值标注
- 达标率统计
- 医学建议参考（免责声明）

### 7.6 个人信息模块（V2.0新增）

#### 7.6.1 基础信息

- 姓名
- 性别（男/女/其他）
- 年龄
- 身高（cm）
- 体重（kg）
- 血型（A/B/AB/O）

#### 7.6.2 健康信息

- 常见血压
- 常见血糖
- 慢性病/既往病史
- 过敏史
- 长期用药

#### 7.6.3 健康目标

- 当前健康目标设置
- 目标达成情况跟踪

#### 7.6.4 数据用途

- 健康数据参考值个性化
- 异常判断依据
- 统计分析基础数据

### 7.7 数据导出模块

#### 7.7.1 导出格式

系统支持以下导出格式：

- **JSON**：完整备份数据，包含结构化信息
- **CSV**：表格格式，Excel友好

#### 7.7.2 导出范围（V2.0增强）

**当前实现：**
- 按当前所选日期导出
- 包含当日活动和健康数据

**V2.0新增：**
- **日期范围导出**：选择起止日期
- **全量导出**：导出所有历史数据
- **数据类型选择**：活动数据、健康数据、提醒数据、个人信息
- **导出预览**：导出前显示数据概览

#### 7.7.3 导出入口

系统应提供明确可见的导出入口：

- 首页顶部"导出"按钮
- 时间线区域"导出今日记录"按钮
- 底部快捷导出按钮

#### 7.7.4 导出内容

**JSON格式包含：**
- 导出时间戳
- 日期范围
- 活动记录数组
- 健康数据数组
- 提醒记录数组（V2.0新增）
- 个人信息（V2.0新增）

**CSV格式包含：**
- 活动数据表
- 健康数据表
- 提醒数据表（V2.0新增）

#### 7.7.5 导出文件命名

格式：`daily-tracker-YYYY-MM-DD.json` 或 `daily-tracker-YYYY-MM-DD.csv`

多日导出：`daily-tracker-YYYY-MM-DD-to-YYYY-MM-DD.json`

### 7.8 数据导入模块（V2.0新增）

#### 7.8.1 导入入口

- 设置页面"数据管理"区域
- 支持拖拽文件上传
- 支持点击选择文件

#### 7.8.2 导入格式

仅支持JSON格式导入（CSV为只读格式）

#### 7.8.3 导入流程

1. 选择JSON文件
2. 系统验证文件格式
3. 显示导入预览：
   - 数据日期范围
   - 活动记录数量
   - 健康数据数量
   - 提醒数量
4. 选择导入策略：
   - **合并导入**：保留现有数据，新增导入数据（冲突时覆盖）
   - **替换导入**：清空现有数据，完全替换
5. 确认导入
6. 显示导入结果

#### 7.8.4 导入校验

- 文件格式校验
- 数据完整性校验
- 数据版本兼容性检查
- 异常数据提示

#### 7.8.5 导入安全

- 导入前自动备份当前数据
- 导入失败可恢复
- 数据冲突处理策略

### 7.9 数据存储模块

#### 7.9.1 存储方式（V2.0增强）

**当前实现：**
- LocalStorage存储
- 容量限制：约5-10MB

**V2.0迁移方案：**
- **主要存储**：IndexedDB
  - 容量更大（通常50MB-无限制）
  - 支持索引查询
  - 支持事务
- **降级方案**：LocalStorage（兼容旧浏览器）
- **图片存储**：IndexedDB Blob存储，对象URL显示

#### 7.9.2 数据结构

**活动数据：**
```javascript
{
  id: string,
  time: string,
  type: 'meal' | 'medication' | 'exercise' | 'sleep' | 'work' | 'other',
  content: string,
  feeling: string,
  duration: number | null,
  image: Blob | null, // V2.0改为Blob
  createdAt: string,
  updatedAt: string // V2.0新增
}
```

**健康数据：**
```javascript
{
  id: string,
  time: string,
  type: 'bloodPressure' | 'heartRate' | 'bloodSugar' | 'bloodLipid' | 'uricAcid' | 'other',
  value: string,
  unit: string,
  notes: string,
  image: Blob | null,
  createdAt: string,
  updatedAt: string
}
```

**提醒数据：**
```javascript
{
  id: string,
  title: string,
  type: string,
  date: string,
  time: string,
  repeat: boolean,
  notes: string,
  image: Blob | null,
  completed: boolean,
  completedAt: string | null,
  snoozeUntil: string | null,
  snoozeCount: number, // V2.0新增
  createdAt: string,
  updatedAt: string
}
```

#### 7.9.3 存储容量管理（V2.0新增）

- **容量监控**：实时显示已用空间/总空间
- **容量预警**：使用超过80%时提醒用户
- **清理建议**：显示各类数据占用空间
- **图片压缩**：提供压缩级别选择（高/中/低）
- **旧数据归档**：自动归档90天以前的数据

#### 7.9.4 自动备份（V2.0新增）

- **备份提醒**：每周提醒用户导出备份
- **备份位置**：浏览器本地下载
- **备份内容**：全量数据JSON
- **备份恢复**：导入备份文件恢复

#### 7.9.5 数据迁移（V2.0新增）

- LocalStorage → IndexedDB 自动迁移
- 数据版本管理
- 迁移失败回滚
- 迁移进度显示

### 7.10 PWA 离线功能模块（V2.0新增）

#### 7.10.1 Service Worker

- 注册Service Worker
- 缓存静态资源（HTML/CSS/JS）
- 缓存用户数据（IndexedDB）
- 离线访问支持

#### 7.10.2 离线能力

- **离线浏览**：无网络时可访问应用
- **离线记录**：离线时可新增活动
- **离线提醒**：Service Worker后台提醒（需浏览器支持）
- **数据同步**：联网后自动同步

#### 7.10.3 安装体验

- 添加到主屏幕提示
- 安装引导教程
- 应用图标优化（本地图标）
- 启动画面优化

### 7.11 长辈模式模块（已实现）

#### 7.11.1 长辈模式功能

- **大字体**：全局字体放大至18px
- **大按钮**：按钮尺寸增大至68px
- **大图标**：统计图标放大至2.8rem
- **高对比度**：增强文字对比度
- **简化布局**：减少信息密度

#### 7.11.2 切换方式

- 顶部"长辈模式"按钮切换
- 状态保存到LocalStorage

### 7.12 用户体验增强模块（V2.0新增）

#### 7.12.1 图片功能增强

- **图片预览**：点击图片放大查看
- **图片裁剪**：上传时裁剪功能
- **图片压缩级别**：用户可选择压缩质量
- **图片批量上传**：一次选择多张图片

#### 7.12.2 暗色模式

- 暗色主题切换
- 跟随系统主题
- 主题偏好保存

#### 7.12.3 首次使用引导

- 欢迎页面
- 功能介绍轮播
- 快速上手教程
- 示例数据加载

#### 7.12.4 加载状态

- 页面加载动画
- 数据加载进度
- 操作处理中提示
- 骨架屏（可选）

#### 7.12.5 错误处理

- 友好的错误提示
- 错误恢复建议
- 操作撤销功能
- 异常数据修复

#### 7.12.6 快捷操作

- 快捷键支持（Ctrl+N新增活动，Esc关闭弹窗）
- 手势操作（左右滑动切换日期）
- 长按快捷菜单
- 语音输入（可选）

## 8. 交互与体验要求

### 8.1 设计原则

- 简洁清晰：界面元素精简，信息层级清晰
- 移动优先：以手机端为核心设计，兼顾桌面端
- 操作路径短：核心功能1-3步完成
- 学习成本低：直观易懂，无学习门槛
- 反馈及时：操作后立即给予反馈

### 8.2 关键交互要求

**导航交互：**
- 主要操作按钮固定在页面底部，便于手机单手操作
- 弹窗支持点击关闭按钮、取消按钮、遮罩区关闭
- 首页提醒摘要支持点击后进入提醒管理弹窗

**快捷键支持：**
- `Ctrl + N`：新增活动
- `Esc`：关闭弹窗
- V2.0新增：
  - `Ctrl + E`：快速编辑最近活动
  - `Ctrl + S`：保存当前编辑
  - `Left/Right`：切换日期

**手势操作（V2.0新增）：**
- 左右滑动：切换日期
- 长按：快捷菜单（编辑/删除）
- 下拉刷新：重新加载数据
- 双击：快速操作

**反馈机制：**
- 用户提交后应有 Toast 反馈
- 加载过程显示加载动画
- 错误操作给出明确提示
- V2.0新增：操作撤销功能（30秒内）

### 8.3 响应式要求

**移动端（主要）：**
- 核心信息优先在 360px 以上宽度正常展示
- 最小支持宽度 320px
- 单列布局，便于浏览

**平板端：**
- 支持宽度 768px - 1024px
- 双列布局优化
- 利用宽屏优势展示更多信息

**桌面端：**
- 支持宽度 1024px 以上
- 最大宽度限制 1200px，居中显示
- 优化键盘操作体验

### 8.4 可访问性要求（V2.0新增）

- 符合WCAG 2.1 AA级标准
- 支持屏幕阅读器
- 键盘导航完整支持
- 颜色对比度符合标准
- 文字可缩放至200%

### 8.5 性能体验要求（V2.0新增）

**加载性能：**
- 首屏加载时间 < 1秒
- 交互响应时间 < 100ms
- 图片懒加载
- 虚拟滚动（长列表）

**操作性能：**
- 防抖节流避免频繁操作
- 数据缓存减少重复计算
- 异步处理不阻塞UI
- 图片处理使用Web Worker

**存储性能：**
- IndexedDB异步读写
- 批量操作优化
- 索引优化查询速度

## 9. 非功能需求

### 9.1 性能要求

**当前实现：**
- 首屏快速加载，无需构建与接口请求
- 本地数据读写应在用户体感上接近实时

**V2.0增强：**
- 首屏加载时间 < 1秒（3G网络）
- 时间交互响应时间 < 100ms
- 图片加载采用懒加载
- 长列表采用虚拟滚动
- 内存占用 < 100MB
- 支持大数据量（10000+条记录）流畅运行

### 9.2 可用性要求

**当前实现：**
- 首次打开即可使用
- 新增活动流程应控制在 3 步以内完成
- 进入提醒管理流程应控制在 1 步内完成
- 进入导出选择界面应控制在 2 步内完成
- 主要功能无需登录即可访问

**V2.0增强：**
- 首次使用提供引导教程
- 关键操作提供帮助提示
- 错误信息通俗易懂
- 提供操作撤销功能
- 支持离线使用
- 数据自动保存，防止意外丢失

### 9.3 兼容性要求

**当前实现：**
- 支持现代移动浏览器
- 支持现代桌面浏览器
- 通知能力依赖浏览器是否支持 Notification API

**V2.0增强：**

**浏览器支持：**
- Chrome 80+（推荐）
- Safari 13+
- Firefox 75+
- Edge 80+
- 移动端Safari（iOS 13+）
- 移动端Chrome（Android 8+）

**特性检测与降级：**
- IndexedDB支持检测，降级到LocalStorage
- Service Worker支持检测，离线功能降级
- Notification API支持检测，提醒功能降级
- Web Worker支持检测，图片处理降级

**屏幕适配：**
- 最小宽度：320px
- 最大宽度：无限制（内容区最大1200px）
- 支持横屏显示（优化布局）

### 9.4 隐私与安全要求

**当前实现：**
- 不采集用户账号信息
- 不上传健康记录到服务器
- 不依赖第三方登录

**V2.0增强：**

**数据安全：**
- 敏感健康数据加密存储（可选功能）
- XSS防护（已实现escapeHtml）
- 数据导入校验，防止恶意文件
- 图片文件类型和大小校验

**网络安全：**
- 建议HTTPS部署
- 设置Content-Security-Policy
- 不引用外部资源（图标本地化）

**隐私保护：**
- 数据完全本地化，不上传
- 导出文件包含隐私提醒
- 清空数据需二次确认
- 可选数据锁功能（PIN码）

**用户知情：**
- 明确告知数据存储位置
- 明确告知数据丢失风险
- 定期提醒用户备份
- 提供数据导出和删除功能

### 9.5 可维护性要求（V2.0新增）

**代码质量：**
- JavaScript模块化，按功能拆分文件
- 减少全局变量，使用命名空间
- 代码注释清晰，关键逻辑有文档
- 统一代码风格（ESLint + Prettier）

**测试覆盖：**
- 单元测试覆盖率 > 60%
- 关键函数测试覆盖率 > 80%
- E2E测试覆盖核心流程
- 浏览器兼容性测试

**文档完善：**
- README更新维护
- API文档（如有后端）
- 部署文档
- 变更日志

### 9.6 可扩展性要求（V2.0新增）

**架构设计：**
- 模块化设计，低耦合
- 数据层与UI层分离
- 支持插件扩展（可选）
- 支持多语言（国际化预留）

**数据扩展：**
- 数据模型可扩展字段
- 支持自定义活动类型（可选）
- 支持自定义健康指标（可选）
- 数据导入导出格式向后兼容

**功能扩展：**
- 预留云同步接口
- 预留第三方数据源接入接口
- 预留分享功能接口
- 预留统计分析扩展接口

## 10. 已知限制与解决方案

### 10.1 当前版本（V1.0）已知限制

以下内容属于当前版本已知限制，应在产品说明中明确告知用户：

1. **提醒功能限制**
   - 问题：提醒功能依赖页面处于运行状态，关闭浏览器后无法保证触发提醒
   - 影响：用户可能错过重要提醒
   - V2.0解决：Service Worker后台推送

2. **PWA能力不完整**
   - 问题：当前仅声明了PWA能力，但离线缓存能力未完整实现
   - 影响：无法离线使用，网络断开时应用不可用
   - V2.0解决：完整实现Service Worker离线缓存

3. **导出范围有限**
   - 问题：导出能力当前仅覆盖当前所选日期的活动数据与健康数据，不覆盖提醒数据与全量历史数据
   - 影响：无法完整备份所有数据
   - V2.0解决：支持全量导出和范围导出

4. **提醒无法编辑**
   - 问题：当前版本不支持提醒编辑
   - 影响：提醒创建后只能删除重建
   - V2.0解决：实现提醒编辑功能

5. **数据无法导入**
   - 问题：当前版本不支持数据导入
   - 影响：数据备份后无法恢复，更换设备数据丢失
   - V2.0解决：实现数据导入功能

6. **多设备不同步**
   - 问题：当前版本不支持多设备同步
   - 影响：更换设备数据无法自动迁移
   - V2.0解决：数据导入导出手动迁移（暂不实现云同步）

7. **存储容量限制**
   - 问题：LocalStorage容量限制约5-10MB，图片存储快速耗尽空间
   - 影响：数据量大时无法继续记录
   - V2.0解决：迁移到IndexedDB存储

8. **缺少数据验证**
   - 问题：健康数据无范围校验，可能输入不合理数值
   - 影响：数据质量无法保证
   - V2.0解决：实现数据校验和异常提示

9. **统计功能薄弱**
   - 问题：只有当日统计，无历史趋势分析
   - 影响：无法查看长期健康趋势
   - V2.0解决：增加周/月统计和趋势图表

10. **搜索功能缺失**
    - 问题：无法搜索历史记录
    - 影响：查找特定记录困难
    - V2.0解决：实现全文搜索和筛选功能

### 10.2 V2.0版本限制

以下为V2.0版本仍存在的限制，需在未来版本解决：

1. **无云同步**
   - 限制：仍不支持自动云同步
   - 影响：多设备数据需手动导入导出
   - 解决方案：V3.0考虑可选云备份服务

2. **无账号体系**
   - 限制：无用户账号系统
   - 影响：无法实现云同步、多用户协作
   - 解决方案：保持轻量级定位，暂不实现

3. **无社交功能**
   - 限制：无分享、协作功能
   - 影响：无法与他人共享健康数据
   - 解决方案：V2.x版本考虑分享功能

4. **无第三方数据接入**
   - 限制：不支持智能手环、健康设备数据导入
   - 影响：需要手动录入所有数据
   - 解决方案：V3.0考虑开放API接口

5. **无医疗诊断**
   - 限制：仅提供数据记录，不提供医疗诊断建议
   - 影响：用户需自行判断健康状况
   - 解决方案：保持产品定位，避免医疗风险

## 11. 用户故事

### 11.1 核心用户故事（V1.0）

1. 作为一名需要规律吃药的用户，我希望设置每日重复提醒，以便我不漏服药物。
2. 作为一名关注饮食健康的用户，我希望记录每次用餐内容和感受，以便我回顾哪些食物更适合自己。
3. 作为一名坚持运动的用户，我希望记录运动次数和时长，以便我查看每天是否达成目标。
4. 作为一名重视隐私的用户，我希望数据仅保存在本地浏览器中，以便避免上传个人健康信息。
5. 作为一名需要备份数据的用户，我希望能导出 JSON 或 CSV，以便后续保存或分析。

### 11.2 V2.0新增用户故事

**数据管理场景：**

6. 作为一名长期使用应用的用户，我希望能够导入之前导出的数据，以便我在更换设备后恢复所有记录。
7. 作为一名数据量大的用户，我希望应用能提示我存储空间使用情况，以便我及时清理或备份数据。
8. 作为一名谨慎的用户，我希望在数据丢失前收到备份提醒，以便我定期保存重要记录。

**提醒管理场景：**

9. 作为一名需要灵活调整提醒的用户，我希望能够编辑已创建的提醒，以便我修改提醒时间或内容而无需删除重建。
10. 作为一名忙碌的用户，我希望能够延后提醒并自定义延后时间，以便我在合适的时间处理提醒。
11. 作为一名需要查看历史的用户，我希望看到已完成的提醒记录，以便我回顾过去的提醒完成情况。

**健康数据场景：**

12. 作为一名高血压患者，我希望记录血压数据时系统自动校验数值合理性，以便我避免录入错误数据。
13. 作为一名关注血糖的用户，我希望看到血糖数据的变化趋势图，以便我了解饮食对血糖的影响。
14. 作为一名需要就医的用户，我希望导出包含健康数据的完整报告，以便我向医生展示我的健康记录。

**统计分析场景：**

15. 作为一名目标导向的用户，我希望看到周/月的运动统计，以便我评估是否达成运动目标。
16. 作为一名数据驱动的用户，我希望看到各类型活动的分布图表，以便我了解自己的时间分配。
17. 作为一名追求进步的用户，我希望看到本周与上周的数据对比，以便我激励自己持续改进。

**搜索与筛选场景：**

18. 作为一名需要查找记录的用户，我希望能够搜索活动内容，以便我快速找到特定记录。
19. 作为一名只看运动记录的用户，我希望能够按类型筛选活动，以便我专注于运动数据。
20. 作为一名需要回顾历史的用户，我希望能够按日期范围查询，以便我查看特定时间段的所有记录。

**用户体验场景：**

21. 作为一名老年用户，我希望使用长辈模式，以便我能更清楚地看到界面内容并轻松操作。
22. 作为一名夜间使用的用户，我希望有暗色模式，以便我在昏暗环境下更舒适地使用应用。
23. 作为一名首次使用的用户，我希望看到功能引导，以便我快速了解如何使用应用。
24. 作为一名查看图片的用户，我希望点击图片可以放大查看，以便我看清图片细节。

**数据导入导出场景：**

25. 作为一名更换手机的用户，我希望导出所有历史数据并导入到新设备，以便我无缝继续使用。
26. 作为一名需要分享数据的用户，我希望导出PDF报告，以便我方便地分享给家人或医生。
27. 作为一名只导出部分数据的用户，我希望选择日期范围导出，以便我只备份需要的数据。

### 11.3 用户故事优先级

**P0（必须实现）：**
- 用户故事 6, 7, 8（数据管理）
- 用户故事 9（提醒编辑）
- 用户故事 12（数据校验）

**P1（应该实现）：**
- 用户故事 10, 11（提醒增强）
- 用户故事 13, 14（健康数据增强）
- 用户故事 15, 16, 17（统计分析）
- 用户故事 18, 19, 20（搜索筛选）
- 用户故事 25（数据导入导出）

**P2（可以实现）：**
- 用户故事 21, 22, 23, 24（用户体验）
- 用户故事 26, 27（导出增强）

## 12. 验收标准

### 12.1 活动记录验收

**基础功能：**
- ✅ 用户可以新增活动并立即在时间线看到结果
- ✅ 用户可以编辑活动且修改后内容正确更新
- ✅ 用户可以删除活动且删除后统计同步更新
- ✅ 空白日期能展示空状态

**V2.0验收标准：**
- ✅ 用户可以上传图片并正常显示
- ✅ 图片压缩后文件大小合理（<500KB）
- ✅ 时间输入有合理性校验，未来时间给予提示
- ✅ 支持正序/倒序切换
- ✅ 支持全文搜索并能正确返回结果
- ✅ 支持按类型筛选，筛选结果准确
- ✅ 支持按日期范围查询，结果完整
- ✅ 长列表滚动流畅（1000+条记录）
- ✅ 删除操作支持撤销（30秒内）

### 12.2 健康数据验收（V2.0）

**数据记录：**
- ✅ 用户可以记录各类健康指标
- ✅ 单位自动填充正确
- ✅ 血压格式验证（收缩压/舒张压）
- ✅ 数值范围校验，异常值给予提示
- ✅ 健康数据在时间线正确显示
- ✅ 健康数据卡片显示最新值

**数据展示：**
- ✅ 健康数据与活动数据混合展示正确
- ✅ 健康数据有特殊标识（🩺图标）
- ✅ 健康趋势图表显示正确
- ✅ 异常值在图表中标注

### 12.3 提醒功能验收

**基础功能：**
- ✅ 用户可以新增提醒并在对应提醒列表中看到
- ✅ 今日提醒和首页提醒摘要展示一致
- ✅ 即将到来列表仅展示最近 10 条未完成提醒，且按时间升序排列
- ✅ 用户可以标记提醒完成
- ✅ 每日重复提醒在完成后能自动生成下一日提醒
- ✅ 用户可以删除提醒

**V2.0验收标准：**
- ✅ 用户可以编辑已创建的提醒
- ✅ 编辑后提醒内容正确更新
- ✅ 用户可以延后提醒并自定义延后时间
- ✅ 延后后提醒时间正确调整
- ✅ 延后次数限制生效（最多3次）
- ✅ 提醒可以搜索并返回正确结果
- ✅ 提醒可以按类型、状态筛选
- ✅ 已完成提醒有历史记录
- ✅ 到点提醒弹窗显示完整信息
- ✅ 提醒支持上传图片

### 12.4 个人信息验收（V2.0）

- ✅ 用户可以填写并保存个人信息
- ✅ 个人信息各项字段显示正确
- ✅ 刷新页面后个人信息仍保留
- ✅ 个人信息导出功能正常

### 12.5 导出功能验收

**基础功能：**
- ✅ 页面存在明确可见的导出入口
- ✅ 用户可进入导出弹窗
- ✅ 用户触发导出后，浏览器可下载 JSON 文件
- ✅ 用户触发导出后，浏览器可下载 CSV 文件
- ✅ 导出内容仅包含当前所选日期的活动数据和健康数据
- ✅ CSV 文件可在 Excel 中正常打开

**V2.0验收标准：**
- ✅ 用户可以选择日期范围导出
- ✅ 用户可以导出全量历史数据
- ✅ 用户可以选择导出数据类型（活动/健康/提醒/个人信息）
- ✅ 导出前显示数据概览（日期范围、数据量）
- ✅ 导出文件命名正确（包含日期范围）
- ✅ JSON格式包含完整数据结构
- ✅ 多日导出文件格式正确

### 12.6 导入功能验收（V2.0）

- ✅ 用户可以选择JSON文件导入
- ✅ 系统验证文件格式并给出提示
- ✅ 导入前显示数据预览
- ✅ 用户可以选择合并或替换导入
- ✅ 合并导入：保留现有数据，新增导入数据
- ✅ 替换导入：清空现有数据，完全替换
- ✅ 导入后数据正确显示
- ✅ 导入失败时可恢复原数据
- ✅ 数据冲突时按策略正确处理
- ✅ 导入异常数据时给予明确提示

### 12.7 存储验收

**基础功能：**
- ✅ 刷新页面后，活动数据和提醒数据仍可保留
- ✅ 切换日期后，可看到对应日期的历史记录

**V2.0验收标准：**
- ✅ IndexedDB存储正常工作
- ✅ 降级到LocalStorage时功能正常（兼容旧浏览器）
- ✅ 存储容量实时监控显示
- ✅ 容量超过80%时给予预警
- ✅ 图片存储为Blob格式
- ✅ 数据查询使用索引，性能良好
- ✅ LocalStorage → IndexedDB迁移功能正常
- ✅ 迁移失败时可回滚

### 12.8 搜索与筛选验收（V2.0）

- ✅ 全文搜索返回正确结果
- ✅ 搜索结果高亮显示关键词
- ✅ 无结果时显示友好提示
- ✅ 按类型筛选结果准确
- ✅ 按日期范围查询结果完整
- ✅ 组合条件查询逻辑正确
- ✅ 搜索和筛选可组合使用
- ✅ 清除筛选条件功能正常

### 12.9 统计验收（V2.0）

**当日统计：**
- ✅ 统计数据实时更新
- ✅ 运动时长正确显示（小时+分钟）

**周/月统计：**
- ✅ 周统计数据显示正确
- ✅ 月统计数据显示正确
- ✅ 环比数据计算准确
- ✅ 统计图表渲染正确

**趋势图表：**
- ✅ 折线图显示趋势变化
- ✅ 柱状图显示时长分布
- ✅ 饼图显示活动类型分布
- ✅ 图表交互正常（悬停显示数值）

### 12.10 用户体验验收（V2.0）

**图片功能：**
- ✅ 图片点击可放大预览
- ✅ 预览时可缩放和关闭
- ✅ 图片裁剪功能正常

**暗色模式：**
- ✅ 暗色模式切换正常
- ✅ 跟随系统主题功能正常
- ✅ 主题偏好保存后刷新仍保留

**首次使用引导：**
- ✅ 首次打开显示欢迎页面
- ✅ 功能介绍轮播正常
- ✅ 引导完成后不再显示
- ✅ 可重新查看引导

**加载状态：**
- ✅ 页面加载时显示加载动画
- ✅ 数据加载显示进度
- ✅ 操作处理中有明确提示

**错误处理：**
- ✅ 错误提示友好易懂
- ✅ 错误恢复建议明确
- ✅ 操作撤销功能正常

### 12.11 PWA离线功能验收（V2.0）

- ✅ Service Worker成功注册
- ✅ 静态资源被缓存
- ✅ 离线时可访问应用
- ✅ 离线时可新增活动
- ✅ 联网后数据同步正常
- ✅ 安装提示正常显示
- ✅ 添加到主屏幕功能正常

### 12.12 长辈模式验收

- ✅ 长辈模式切换正常
- ✅ 字体放大至18px
- ✅ 按钮尺寸增大
- ✅ 图标放大
- ✅ 文字对比度增强
- ✅ 刷新后模式设置保留

### 12.13 性能验收（V2.0）

- ✅ 首屏加载时间 < 1秒（3G网络）
- ✅ 交互响应时间 < 100ms
- ✅ 1000条记录滚动流畅
- ✅ 内存占用 < 100MB
- ✅ 图片懒加载正常
- ✅ 防抖节流避免频繁操作

### 12.14 兼容性验收（V2.0）

- ✅ Chrome 80+功能正常
- ✅ Safari 13+功能正常
- ✅ Firefox 75+功能正常
- ✅ Edge 80+功能正常
- ✅ 移动端Safari功能正常
- ✅ 移动端Chrome功能正常
- ✅ 不支持IndexedDB时降级正常
- ✅ 不支持Service Worker时功能降级

### 12.15 安全验收（V2.0）

- ✅ XSS防护有效（用户输入已转义）
- ✅ 数据导入校验有效
- ✅ 图片文件类型校验
- ✅ 图片文件大小限制生效
- ✅ 敏感数据可选加密存储
- ✅ CSP策略配置正确（如有）
- ✅ 清空数据需二次确认

## 13. 版本规划

### 13.1 V2.0 - 原型完善版（当前阶段）

**开发周期：** 4-6周

**目标：** 解决核心痛点，完善产品功能，提升数据安全性

**P0功能（必须完成）：**

**第1-2周：数据持久化增强**
- 数据导入功能（JSON恢复）
- 迁移至IndexedDB存储
- 存储容量监控与预警
- 自动备份提醒机制

**第3-4周：提醒功能完善**
- 提醒编辑功能
- 提醒延后自定义
- 提醒历史记录
- 提醒搜索与筛选

**第5-6周：数据验证与PWA**
- 健康数据范围校验
- 时间合理性检查
- 重复提交防护
- Service Worker离线缓存

**验收标准：**
- 所有P0功能验收通过
- 数据不再因容量限制丢失
- 提醒可编辑，提醒功能完善
- 支持离线访问

### 13.2 V2.1 - 数据分析版

**开发周期：** 2-3周

**目标：** 增强数据统计和分析能力

**P1功能：**

**周/月统计：**
- 本周/本月活动统计
- 环比数据对比
- 平均活动次数

**数据可视化：**
- 活动次数趋势图
- 运动时长趋势图
- 健康数据变化曲线
- 活动类型分布饼图

**搜索与筛选：**
- 历史记录全文搜索
- 按活动类型筛选
- 按日期范围查询
- 组合条件查询

**导出增强：**
- 全量数据导出
- 日期范围导出
- 导出预览
- 提醒数据导出

**验收标准：**
- 统计数据准确
- 图表交互流畅
- 搜索功能正常
- 导出功能完善

### 13.3 V2.2 - 用户体验版

**开发周期：** 2周

**目标：** 提升用户体验和满意度

**P2功能：**

**图片功能：**
- 图片预览放大
- 图片裁剪
- 压缩级别选择

**界面优化：**
- 暗色模式
- 首次使用引导
- 数据为空引导
- 加载状态提示

**用户体验细节：**
- 错误处理优化
- 操作撤销功能
- 手势操作
- 快捷键增强

**验收标准：**
- 用户体验显著提升
- 错误率降低
- 用户满意度提升

### 13.4 V3.0 - 扩展功能版（未来规划）

**开发周期：** 6-8周

**目标：** 提供可选的高级功能

**可选云备份：**
- 可选的云账号注册
- 云端数据加密备份
- 多设备同步（可选）
- 数据恢复

**分享协作：**
- 分享活动记录
- 导出PDF报告
- 打印友好格式
- 家庭成员共享（可选）

**高级分析：**
- 自定义统计周期
- 数据标签系统
- 健康数据异常预警
- AI健康建议（可选）

**第三方接入：**
- 开放API接口
- 智能手环数据导入
- 医院系统对接（可选）

**验收标准：**
- 云备份安全可靠
- 分享功能正常
- 高级分析准确
- API接口稳定

### 13.5 版本迭代原则

**优先级原则：**
- P0 > P1 > P2
- 用户痛点优先
- 数据安全优先
- 核心功能优先

**迭代节奏：**
- 小版本（V2.1/2.2）：2-3周
- 大版本（V3.0）：6-8周
- 修复版本（V2.0.1）：随时发布

**质量要求：**
- 单元测试覆盖率 > 60%
- E2E测试覆盖核心流程
- 性能不倒退
- 兼容性不降低

**发布策略：**
- 灰度发布（先部分用户）
- 收集反馈
- 修复问题
- 全量发布

## 14. 附录

### 14.1 技术方案

**当前技术栈（V1.0）：**
- 前端：HTML + CSS + 原生JavaScript
- 存储：LocalStorage
- 部署：静态文件托管（Vercel / GitHub Pages）

**V2.0技术方案：**

**前端架构：**
```
推荐方案1（渐进式改进）：
- 模块化JavaScript（ES6 Modules）
- 按功能拆分文件（storage/activity/health/reminder/ui/utils）
- 减少全局变量，使用命名空间
- ESLint + Prettier代码规范
- Jest单元测试

推荐方案2（技术升级）：
- Vue 3 + Composition API
- Pinia状态管理
- Vite构建工具
- TypeScript类型安全
- Vitest测试框架
```

**数据存储：**
```
主存储：IndexedDB
- Dexie.js封装库（推荐）
- 数据库结构：
  - activities（活动表，时间索引）
  - healthRecords（健康数据表，时间索引）
  - reminders（提醒表，时间索引）
  - profile（个人信息表）
  - settings（设置表）

降级方案：LocalStorage
- 兼容不支持IndexedDB的浏览器
- 自动降级逻辑

图片存储：
- IndexedDB Blob存储
- 对象URL显示
- 压缩策略：最大1600px，JPEG质量0.82
```

**PWA实现：**
```javascript
Service Worker功能：
- 静态资源缓存
- 离线访问支持
- 后台数据同步
- 推送通知（需HTTPS）

Workbox配置：
- 缓存策略配置
- 预缓存资源列表
- 运行时缓存规则
```

**性能优化：**
```
图片处理：
- Web Worker异步压缩
- 懒加载
- 响应式图片

列表优化：
- 虚拟滚动
- 防抖节流
- DocumentFragment批量DOM操作

数据优化：
- 索引查询
- 批量操作
- 数据压缩
```

### 14.2 数据模型

**完整数据模型（V2.0）：**

```javascript
// 活动数据
interface Activity {
  id: string;                    // 唯一标识
  time: string;                  // 时间 HH:mm
  type: ActivityType;            // 类型
  content: string;               // 内容
  feeling?: string;              // 感受
  duration?: number;             // 时长（分钟）
  image?: Blob;                  // 图片（V2.0改为Blob）
  date: string;                  // 日期 YYYY-MM-DD
  createdAt: string;             // 创建时间
  updatedAt: string;             // 更新时间（V2.0新增）
  tags?: string[];               // 标签（V2.2可选）
}

type ActivityType = 'meal' | 'medication' | 'exercise' | 'sleep' | 'work' | 'other';

// 健康数据
interface HealthRecord {
  id: string;
  time: string;
  type: HealthType;
  value: string;                 // 数值
  unit: string;                  // 单位
  notes?: string;                // 备注
  image?: Blob;
  date: string;
  createdAt: string;
  updatedAt: string;
}

type HealthType = 'bloodPressure' | 'heartRate' | 'bloodSugar' | 'bloodLipid' | 'uricAcid' | 'other';

// 提醒数据
interface Reminder {
  id: string;
  title: string;                 // 提醒内容
  type: ActivityType;            // 类型
  date: string;                  // 日期
  time: string;                  // 时间
  repeat: boolean;               // 是否每日重复
  notes?: string;                // 备注
  image?: Blob;                  // 图片
  completed: boolean;            // 是否完成
  completedAt?: string;          // 完成时间
  snoozeUntil?: string;          // 延后到
  snoozeCount: number;           // 延后次数（V2.0新增）
  createdAt: string;
  updatedAt: string;
}

// 个人信息
interface Profile {
  name?: string;
  gender?: 'male' | 'female' | 'other';
  age?: number;
  height?: number;               // cm
  weight?: number;               // kg
  bloodType?: 'A' | 'B' | 'AB' | 'O';
  bloodPressure?: string;        // 常见血压
  bloodSugar?: string;           // 常见血糖
  chronicConditions?: string;    // 慢性病
  allergies?: string;            // 过敏史
  medications?: string;          // 长期用药
  healthGoals?: string;          // 健康目标
  notes?: string;                // 备注
  createdAt: string;
  updatedAt: string;
}

// 应用设置
interface Settings {
  elderMode: boolean;            // 长辈模式
  darkMode: boolean;             // 暗色模式
  timelineOrder: 'asc' | 'desc'; // 时间线排序
  autoBackup: boolean;           // 自动备份
  backupInterval: number;        // 备份间隔（天）
  notificationEnabled: boolean;  // 通知开关
  imageQuality: 'high' | 'medium' | 'low'; // 图片质量
  storageQuota: number;          // 存储容量（字节）
  lastBackupTime?: string;       // 最后备份时间
  createdAt: string;
  updatedAt: string;
}

// 数据版本（用于迁移）
interface DataVersion {
  version: string;
  lastMigration: string;
  createdAt: string;
}
```

### 14.3 API设计（内部接口）

**数据操作API：**

```javascript
// 活动相关
ActivityAPI.create(activity: Activity): Promise<Activity>
ActivityAPI.update(id: string, updates: Partial<Activity>): Promise<Activity>
ActivityAPI.delete(id: string): Promise<void>
ActivityAPI.getById(id: string): Promise<Activity | null>
ActivityAPI.getByDate(date: string): Promise<Activity[]>
ActivityAPI.getByRange(startDate: string, endDate: string): Promise<Activity[]>
ActivityAPI.search(query: string): Promise<Activity[]>
ActivityAPI.filter(type?: ActivityType, startDate?: string, endDate?: string): Promise<Activity[]>

// 健康数据相关
HealthAPI.create(record: HealthRecord): Promise<HealthRecord>
HealthAPI.update(id: string, updates: Partial<HealthRecord>): Promise<HealthRecord>
HealthAPI.delete(id: string): Promise<void>
HealthAPI.getByDate(date: string): Promise<HealthRecord[]>
HealthAPI.getLatestByType(type: HealthType): Promise<HealthRecord | null>
HealthAPI.getTrend(type: HealthType, days: number): Promise<HealthRecord[]>

// 提醒相关
ReminderAPI.create(reminder: Reminder): Promise<Reminder>
ReminderAPI.update(id: string, updates: Partial<Reminder>): Promise<Reminder>
ReminderAPI.delete(id: string): Promise<void>
ReminderAPI.complete(id: string): Promise<Reminder>
ReminderAPI.snooze(id: string, minutes: number): Promise<Reminder>
ReminderAPI.getToday(): Promise<Reminder[]>
ReminderAPI.getUpcoming(limit: number): Promise<Reminder[]>
ReminderAPI.search(query: string): Promise<Reminder[]>

// 导入导出相关
ExportAPI.exportJSON(options: ExportOptions): Promise<Blob>
ExportAPI.exportCSV(options: ExportOptions): Promise<Blob>
ImportAPI.importJSON(file: File, strategy: 'merge' | 'replace'): Promise<ImportResult>

// 存储相关
StorageAPI.getUsage(): Promise<StorageUsage>
StorageAPI.clearOldData(days: number): Promise<void>
StorageAPI.migrateFromLocalStorage(): Promise<void>
```

### 14.4 错误码定义

```javascript
const ErrorCodes = {
  // 存储相关
  STORAGE_QUOTA_EXCEEDED: 'E001',     // 存储空间不足
  STORAGE_NOT_AVAILABLE: 'E002',      // 存储不可用
  STORAGE_MIGRATION_FAILED: 'E003',   // 数据迁移失败
  
  // 数据相关
  DATA_NOT_FOUND: 'E101',             // 数据不存在
  DATA_INVALID: 'E102',               // 数据格式错误
  DATA_VALIDATION_FAILED: 'E103',     // 数据校验失败
  DATA_IMPORT_FAILED: 'E104',         // 数据导入失败
  DATA_EXPORT_FAILED: 'E105',         // 数据导出失败
  
  // 图片相关
  IMAGE_TOO_LARGE: 'E201',            // 图片过大
  IMAGE_TYPE_NOT_SUPPORTED: 'E202',   // 图片类型不支持
  IMAGE_COMPRESS_FAILED: 'E203',      // 图片压缩失败
  
  // 提醒相关
  REMINDER_TIME_PASSED: 'E301',       // 提醒时间已过
  REMINDER_SNOOZE_LIMIT: 'E302',      // 延后次数超限
  REMINDER_NOTIFICATION_DENIED: 'E303', // 通知权限拒绝
  
  // 其他
  UNKNOWN_ERROR: 'E999'               // 未知错误
};
```

### 14.5 相关文档

**项目文档：**
- `README.md` - 项目说明
- `HOW_TO_USE.md` - 使用指南
- `HEALTH_GUIDE.md` - 健康指南
- `REMINDER_GUIDE.md` - 提醒功能指南
- `VERCEL_DEPLOY.md` - Vercel部署指南
- `GITHUB_DEPLOY.md` - GitHub Pages部署指南
- `快速部署到Vercel.md` - 快速部署教程

**设计文档：**
- UI设计稿（如有）
- 交互流程图（如有）
- 数据库设计图（如有）

**开发文档：**
- API文档
- 数据模型文档
- 开发规范文档

### 14.6 术语表

| 术语 | 说明 |
|------|------|
| Activity | 活动，指用户记录的用餐、用药、运动等行为 |
| Health Record | 健康数据，指血压、血糖等健康指标记录 |
| Reminder | 提醒，用户设置的定时提醒 |
| Profile | 个人信息，用户的基础健康信息 |
| LocalStorage | 浏览器本地存储，容量约5-10MB |
| IndexedDB | 浏览器数据库存储，容量更大，支持索引 |
| PWA | Progressive Web App，渐进式Web应用 |
| Service Worker | PWA核心技术，支持离线缓存和后台任务 |
| Blob | Binary Large Object，二进制大对象，用于存储图片 |
| 虚拟滚动 | 长列表性能优化技术，只渲染可见区域 |
| 防抖节流 | 减少函数执行频率的性能优化技术 |

### 14.7 更新日志

**V2.0 (2026-03-10) - 原型完善版**
- 新增数据导入功能
- 迁移至IndexedDB存储
- 新增提醒编辑功能
- 新增数据校验功能
- 完善PWA离线能力
- 新增搜索与筛选功能
- 新增周/月统计功能
- 新增数据可视化图表
- 新增用户体验增强功能
- 完善文档和验收标准

**V1.0 (2026-03-09) - 初始版本**
- 活动记录基础功能
- 提醒基础功能
- 健康数据记录
- 个人信息管理
- 数据导出功能
- 长辈模式

---

**文档结束**

本文档将随产品迭代持续更新。如有疑问或建议，请联系产品负责人。
