# 健康生活记录器 PRD

## 1. 文档信息

- 产品名称：健康生活记录器（Daily Tracker）
- 文档类型：正式版 PRD
- 文档版本：V4.0
- 文档日期：2026-03-14
- 产品形态：移动优先的单页 Web 应用（PWA）+ 云端同步后端
- 当前状态：高保真原型已实现，含完整后端 API，文档以当前仓库实现为基线

## 2. 产品概述

### 2.1 产品定位

健康生活记录器是一款面向个人用户（尤其是慢性病患者、中老年人群）的轻量健康管理工具，帮助用户完成日常活动记录、健康指标记录、症状跟踪、提醒管理、数据分析、AI 健康诊断，并支持本地离线使用与云端同步。

### 2.2 产品目标

- 让用户低成本记录饮食、用药、运动、睡眠和身体感受
- 让用户记录身体症状和处理措施，便于就医回顾
- 让用户通过提醒机制维持规律作息和按时用药
- 让用户通过统计和趋势图回顾阶段性健康行为
- 让用户通过本地导入导出保护数据资产
- 让用户通过云同步实现跨设备数据访问
- 让用户通过 AI 诊断获得个性化健康分析建议

### 2.3 核心原则

- 本地优先：数据默认保存在本地，可选云端同步
- 移动端优先：面向手机场景设计，兼顾桌面端
- 轻量易用：无框架依赖，操作路径短
- 隐私优先：API 密钥等敏感信息仅存本地
- 离线可用：PWA 支持，断网可用

### 2.4 技术架构

| 层级 | 技术栈 |
|------|--------|
| 前端 | 纯 HTML + CSS + JavaScript（无框架），PWA |
| 图表 | Chart.js 4.4.1 |
| 本地存储 | IndexedDB（主）/ localStorage（降级） |
| 后端 | .NET 8 Minimal API |
| 数据库 | MySQL（腾讯云） |
| 认证 | JWT Bearer Token |

## 3. 目标用户

### 3.1 用户类型

- 有日常健康管理需求的个人用户
- 需要记录用药、饮食、运动和睡眠的用户
- 需要跟踪身体症状变化的慢性病患者
- 需要观察生活方式与身体状态关系的用户
- 对界面清晰度和操作门槛敏感的中老年用户

### 3.2 用户特征

- 更关注简单直接的交互，而非复杂系统能力
- 主要在手机浏览器访问，也会在桌面端使用
- 对个人隐私和数据可控性要求较高
- 可接受本地优先的数据存储方式

## 4. 核心使用场景

- 记录早餐、午餐、晚餐、加餐及餐后感受
- 记录服药时间、药物内容和身体反应
- 记录运动项目、时长和运动后状态
- 记录睡眠、午休和补觉情况
- 记录血压、心率、血糖等健康指标
- 记录突发或持续的身体症状及处理措施
- 以自由文本记录当天的整体状态感受
- 设置定时提醒（用药、运动等）并在到点时处理
- 查看当日时间线和健康数据摘要
- 查看周/月统计及趋势图表
- 导出全部历史数据用于备份或分析
- 导入历史备份恢复数据
- 基于既有记录生成 AI 健康分析并追问
- 通过云同步在多设备间同步数据

## 5. 产品范围

### 5.1 当前版本范围

当前版本包含以下功能域：

1. 日期导航与日历
2. 活动记录与时间线
3. 健康数据记录与摘要
4. 症状记录
5. 今日状态记录
6. 提醒管理与通知
7. 个人健康档案
8. 统计分析与图表
9. 数据导入导出与备份
10. 活动模板
11. AI 健康诊断（含追问）
12. 云同步与账号体系
13. PWA 离线支持

### 5.2 不在当前范围

- 医疗诊断结论
- 在线问诊
- 社交关系和分享社区
- 智能硬件实时接入
- PDF 报告导出

## 6. 信息架构

### 6.1 首页结构

```
├── Header
│   ├── 标题：📅 今日活动记录
│   ├── 工具栏
│   │   ├── 导出按钮
│   │   └── 更多菜单（云同步 | 个人信息 | 示例数据 | 清空数据）
│   └── 日期选择器：◀ [当前日期（点击弹出日历面板）] ▶
│
├── Main
│   ├── 📋 健康数据（可折叠/展开）
│   │   └── 健康摘要网格 + 空状态提示
│   ├── 🔔 今日提醒
│   │   └── 提醒列表 + "查看全部"入口
│   ├── 🧾 今日状态记录
│   │   └── 自由文本 Textarea + 保存 + 一键 AI 诊断
│   └── 📝 时间线
│       └── 活动/健康/症状混合时间线 + 排序切换
│
├── 底部导航
│   ├── ＋ 记录（添加活动）
│   ├── 📋 健康（记录健康数据）
│   ├── 🩹 症状（记录症状）
│   └── 🔔 提醒（提醒管理）
│
└── Modal 弹窗
    ├── 活动记录表单（含快速模板）
    ├── 健康数据表单
    ├── 症状记录表单
    ├── 提醒管理（4 Tab：今日/即将到来/全部/添加）
    ├── 个人信息表单
    ├── AI 健康诊断（含追问对话）
    ├── 数据统计（图表）
    ├── 数据导入/导出
    ├── 云同步配置（登录/同步）
    ├── 模板管理
    ├── 模板编辑
    └── 到点提醒弹窗
```

### 6.2 顶部工具栏

- 桌面端（>600px）：所有工具按钮直接显示在 Header 中
- 移动端（≤600px）：仅显示「导出」按钮 + 「⋮」汉堡菜单（折叠云同步、个人信息、示例数据、清空数据）

### 6.3 底部导航

- 桌面端（>600px）：右下角 FAB 圆形浮动按钮组
- 移动端（≤600px）：全宽底部 TabBar，每个按钮含图标 + 文字标签

### 6.4 Modal 弹窗

- 桌面端：居中弹窗（`margin: 4% auto`）
- 移动端（≤600px）：底部 Sheet 上滑动画，顶部有拖拽手柄

## 7. 功能模块需求

### 7.1 日期导航模块

#### 7.1.1 日期切换

- 点击 ◀/▶ 按钮切换前一天/后一天
- 点击当前日期按钮弹出日历面板

#### 7.1.2 日历面板

- 月份级日历视图，支持按月切换（◀/▶ 月份导航）
- 星期从周一开始显示
- 有记录的日期以圆点标记
- 当前选中日期高亮
- 底部快捷操作：「今天」跳转到今日、「清除」清除选择

### 7.2 活动记录与时间线模块

#### 7.2.1 记录活动

用户可新增和编辑活动，字段包括：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 开始时间 | time | 是 | 活动开始时间 |
| 结束时间 | time | 是 | 活动结束时间 |
| 类型 | select | 是 | 6 种活动类型之一 |
| 内容 | text | 是 | 活动具体内容描述 |
| 感受 | textarea | 否 | 活动后的主观感受 |
| 时长 | number | 否 | 根据起止时间自动计算，只读 |
| 图片 | file | 否 | 支持拍照或相册选择，图片以 Base64 存储 |

活动类型包括：

| 类型 | 图标 | 时间线颜色 |
|------|------|------------|
| 用餐 | 🍽️ | #f59e42（橙色） |
| 用药 | 💊 | #e74c7c（粉红） |
| 运动 | 🏃 | #27ae60（绿色） |
| 睡眠 | 😴 | #6c63d4（紫色） |
| 工作 | 💼 | #2980b9（蓝色） |
| 其他 | 📌 | #95a5a6（灰色） |

#### 7.2.2 活动操作

- 新增活动（底部导航「记录」按钮）
- 编辑活动（时间线条目上的编辑按钮）
- 删除活动（时间线条目上的删除按钮，需二次确认）

#### 7.2.3 时间线展示

时间线按当前选定日期展示活动、健康数据、症状三种记录混合显示。每条记录至少展示：

- 时间
- 类型图标与彩色标识
- 内容摘要
- 感受（如有）
- 时长（如有）
- 图片缩略图（如有，最大高度 160px）
- 编辑 / 删除操作按钮（outline pill 样式）

#### 7.2.4 时间线控制

- 正序 / 倒序切换（偏好持久化到 localStorage）
- 空状态引导文案

### 7.3 健康数据模块

#### 7.3.1 记录健康数据

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 时间 | time | 是 | 测量时间 |
| 指标类型 | select | 是 | 血压/心率/血糖/血脂/尿酸/其他 |
| 数值 | text | 是 | 例如 120/80（血压）或 5.6（血糖） |
| 单位 | text | 否 | 根据指标类型自动填充，可手动修改 |
| 备注 | textarea | 否 | 例如"饭后测量" |
| 图片 | file | 否 | 上传检测报告、仪器屏幕等 |

#### 7.3.2 健康指标类型

| 指标 | 图标 | 默认单位 | 校验范围 |
|------|------|----------|----------|
| 血压 | 🩺 | mmHg | 收缩压 60-250，舒张压 40-150 |
| 心率 | ❤️ | bpm | 30-220 |
| 血糖 | 🩸 | mmol/L | 1-30 |
| 血脂 | 🧪 | mmol/L | 0.5-20 |
| 尿酸 | 💧 | umol/L | 60-1200 |
| 其他 | 📋 | 自定义 | 无限制 |

#### 7.3.3 数据校验

- 血压：校验 `收缩压/舒张压` 格式及合理范围
- 心率、血糖、血脂、尿酸：校验数值在建议范围内
- 输入超出范围时给出提示

#### 7.3.4 数据展示

健康数据在两个区域展示：

- 首页「健康数据」区域：可折叠的健康摘要网格，展示当日各指标最新值
- 时间线：以青蓝色（#1a9bbb）卡片与活动记录混合展示

### 7.4 症状记录模块

#### 7.4.1 记录症状

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 发生时间 | time | 是 | 症状出现的时间 |
| 症状描述 | textarea | 是 | 详细描述症状表现 |
| 处理措施 | textarea | 否 | 描述采取的应对措施 |
| 图片 | file | 否 | 上传症状照片、用药照片等 |

#### 7.4.2 症状展示

- 在时间线中以红色（#e85d5d）卡片展示
- 支持编辑和删除操作

### 7.5 今日状态记录模块

#### 7.5.1 功能描述

以自由文本形式记录当天的整体感受和状态变化。

| 功能 | 说明 |
|------|------|
| 文本输入 | 自由格式 textarea，可记录感受、状态变化、需关注事项 |
| 手动保存 | 点击「保存状态记录」按钮保存 |
| 保存状态 | 显示"尚未保存"或上次保存时间 |
| 一键 AI 诊断 | 快捷入口，直接打开 AI 诊断弹窗 |

### 7.6 提醒管理模块

#### 7.6.1 提醒数据

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 提醒内容 | text | 是 | 提醒的核心内容 |
| 类型 | select | 是 | 用餐/用药/运动/睡眠/其他 |
| 日期 | date | 是 | 提醒日期 |
| 时间 | time | 是 | 提醒时间 |
| 重复规则 | select | 否 | 不重复/每天/每周/每2周/每月 |
| 备注 | textarea | 否 | 补充说明 |
| 图片 | file | 否 | 为提醒附加图片说明 |

#### 7.6.2 提醒视图

提醒管理弹窗包含以下页签：

| Tab | 说明 |
|-----|------|
| 今日提醒 | 显示当天所有提醒，区分已完成/未完成 |
| 即将到来 | 显示未来的提醒 |
| 全部提醒 | 所有提醒的完整列表，支持搜索和筛选 |
| 添加提醒 | 提醒创建/编辑表单 |

「全部提醒」页签顶部集成：

- 关键词搜索（搜索提醒内容和备注）
- 类型筛选（用餐/用药/运动/睡眠/其他）
- 状态筛选（已完成/未完成）

#### 7.6.3 提醒操作

- 新增提醒
- 编辑提醒
- 删除提醒
- 标记完成
- 恢复未完成（重新打开）
- 延后提醒

#### 7.6.4 到点通知

每分钟检查一次到期提醒，到时间弹出提醒弹窗。弹窗展示：

- 提醒类型徽章
- 提醒标题
- 时间信息
- 图片（如有）
- 备注（如有）

弹窗操作：

| 操作 | 说明 |
|------|------|
| 标记完成 | 标记提醒为已完成 |
| 延后提醒 | 预设 10/15/30 分钟或自定义时长（1-240 分钟） |
| 稍后处理 | 关闭弹窗但不标记完成 |

防重复机制：同一 session 内已弹出的提醒不会重复弹出。

#### 7.6.5 系统通知

通过 Service Worker 发送系统级推送通知（需用户授权浏览器通知权限），未授权时仅使用应用内弹窗。

#### 7.6.6 首页提醒摘要

首页「今日提醒」区域展示当天提醒列表，提供「查看全部」入口跳转到提醒管理弹窗。

### 7.7 个人健康档案模块

#### 7.7.1 基础信息

| 字段 | 类型 | 说明 |
|------|------|------|
| 姓名 | text | - |
| 性别 | select | 男/女/其他 |
| 年龄 | number | - |
| 身高 | number | 单位 cm |
| 体重 | number | 单位 kg，支持小数 |
| 血型 | select | A/B/AB/O |

#### 7.7.2 健康背景

| 字段 | 类型 | 说明 |
|------|------|------|
| 常见血压 | text | 例如 120/80 |
| 常见血糖 | text | 例如 5.6 mmol/L |
| 慢性病/既往病史 | textarea | 例如高血压、脂肪肝 |
| 过敏史 | textarea | 例如海鲜过敏、青霉素过敏 |
| 长期用药 | textarea | 例如降压药每日1次 |
| 当前健康目标 | textarea | 例如控制血压、减重 5kg |
| 补充说明 | textarea | 其他需要记录的信息 |

### 7.8 统计分析模块

#### 7.8.1 统计周期

- 本周统计
- 本月统计

#### 7.8.2 统计图表（Chart.js）

| 图表 | 说明 |
|------|------|
| 运动次数趋势 | 按天展示运动次数变化折线图 |
| 单次运动时长趋势 | 展示每次运动的平均时长（分钟/次） |
| 单日睡眠时长趋势 | 展示每日总睡眠时长（小时） |

空状态：选定周期无数据时显示空状态提示。

### 7.9 数据导入导出模块

#### 7.9.1 存储状态展示

- 当前存储引擎类型（IndexedDB / localStorage）
- 存储空间占用情况（进度条 + 文字）
- 最近备份状态
- 备份提醒（超过 7 天未备份时提醒）

#### 7.9.2 导出

| 功能 | 说明 |
|------|------|
| 导出范围 | 当前日期 / 日期范围 / 全部数据 |
| 日期范围 | 选择"日期范围"时显示开始和结束日期选择器 |
| 导出预览 | 显示将导出的活动记录数、健康数据数、提醒数据数 |
| 导出 JSON | 完整数据备份，包含所有记录类型 |
| 导出 CSV | 表格格式，方便在 Excel 中查看 |

#### 7.9.3 导入

| 功能 | 说明 |
|------|------|
| 导入 JSON | 从之前导出的 JSON 文件恢复 |
| 导入预览 | 导入前显示文件内包含的数据量 |
| 合并导入 | 将导入数据与现有数据合并（不覆盖已有记录） |
| 替换导入 | 用导入数据完全替换现有数据 |

导入失败时应保留原始数据。

### 7.10 活动模板模块

#### 7.10.1 预置模板

| 模板名 | 类型 | 内容 | 图标 |
|--------|------|------|------|
| 降压药 | 用药 | 降压药 1片 | 💊 |
| 晨跑 | 运动 | 慢跑 | 🏃 |
| 早餐 | 用餐 | 燕麦粥 + 鸡蛋 | 🍽️ |
| 午休 | 睡眠 | 午休30分钟 | 😴 |

#### 7.10.2 模板功能

| 功能 | 说明 |
|------|------|
| 快速选用 | 在活动表单中点击模板，自动填充类型、内容、感受、时长 |
| 存为模板 | 将当前表单内容保存为新模板 |
| 创建模板 | 通过模板管理界面手动创建 |
| 编辑模板 | 修改已有模板的名称、类型、内容、感受、时长、图标 |
| 删除模板 | 从模板列表中移除 |

#### 7.10.3 模板字段

| 字段 | 必填 | 说明 |
|------|------|------|
| 模板名称 | 是 | - |
| 活动类型 | 是 | 6 种活动类型之一 |
| 内容 | 是 | - |
| 感受 | 否 | - |
| 时长 | 否 | 分钟 |
| 图标 | 是 | 8 种预设图标（💊🏃🍽️😴💼📌⭐❤️） |

### 7.11 AI 健康诊断模块

#### 7.11.1 目标

在本地记录基础上，为用户提供结构化健康总结与建议性分析文本。

**边界说明：** AI 模块仅提供信息整理与建议性分析，不提供医疗诊断结论。

#### 7.11.2 AI 服务配置

| 配置项 | 说明 |
|--------|------|
| AI 服务提供商 | OpenAI / Anthropic / DeepSeek / 通义千问 / 自定义 API |
| API 密钥 | 仅存储在本地浏览器，支持密码显示/隐藏切换 |
| 自定义 API 端点 | 选择「自定义 API」时显示 |
| 模型 | 可选，留空使用默认模型 |
| 测试连接 | 验证 API 配置是否可用 |
| 配置折叠 | 保存后配置区域折叠为摘要，显示"修改配置"按钮 |

#### 7.11.3 数据范围选择

| 选项 | 说明 |
|------|------|
| 今天 | 仅分析当天数据 |
| 最近3天 | - |
| 最近7天 | - |
| 最近30天 | - |
| 最近90天 | - |
| 自定义范围 | 手动指定开始和结束日期 |

选择范围后展示数据预览：活动记录数、健康数据数、分析天数。

#### 7.11.4 诊断选项

可勾选的分析维度：

- 分析活动记录（饮食、运动、睡眠等）
- 分析健康数据（血压、心率、血糖等）
- 结合个人信息（年龄、病史、用药等）
- 分析用药情况

额外关注的问题：自由文本输入，例如"最近血压偏高，想了解原因和建议"。

#### 7.11.5 诊断结果

| 功能 | 说明 |
|------|------|
| 诊断过程 | 在文本框中显示诊断过程和分析依据摘要 |
| 诊断结果 | 以富文本形式展示 AI 分析结论 |
| 复制结果 | 一键复制诊断结果文本 |
| 继续追问 | 诊断完成后进入对话模式，可基于上下文继续提问 |

#### 7.11.6 追问对话

诊断完成后自动显示追问区域：

- 多轮对话消息列表
- 文本输入框 + 发送按钮
- 基于完整对话上下文回复

### 7.12 云同步模块

#### 7.12.1 账号体系

| 功能 | 说明 |
|------|------|
| 注册/登录 | 账号 + 密码登录，未注册时自动创建账号 |
| 密码要求 | 最少 6 位字符 |
| JWT 认证 | 登录成功返回 JWT Token，后续请求自动携带 |
| 退出登录 | 清除本地 Token 和认证状态 |
| 密码显示切换 | 密码输入框支持显示/隐藏 |

#### 7.12.2 同步配置

| 功能 | 说明 |
|------|------|
| 同步服务地址 | 可配置后端 API 地址，有默认值 |
| 登录状态显示 | 显示当前认证状态 |
| 保存配置 | 持久化同步配置到本地 |

#### 7.12.3 同步功能

| 功能 | 说明 |
|------|------|
| 上传到云端 | 将本地数据推送到服务器 |
| 从云端恢复 | 从服务器拉取数据覆盖本地 |
| 自动同步 | 开启后本地保存数据时自动上传到云端 |

#### 7.12.4 云端同步的数据类型

| 数据类型 | 同步方式 |
|----------|----------|
| 活动记录 | 按日期增量 CRUD |
| 健康数据 | 按日期增量 CRUD |
| 症状记录 | 按日期增量 CRUD |
| 提醒数据 | 全量 CRUD + 完成/重启/延后 |
| 个人信息 | 读取/更新 |
| 每日状态记录 | 按日期 CRUD |
| 全量快照 | 上传/下载（旧版兼容） |

### 7.13 PWA 与离线支持

#### 7.13.1 Service Worker

- 缓存应用资源（HTML/CSS/JS/图标等）
- 离线访问支持
- 缓存更新策略：安装时缓存，激活时清理旧版缓存

#### 7.13.2 PWA 能力

| 功能 | 说明 |
|------|------|
| 安装到主屏幕 | 支持 Android/iOS 添加到主屏幕 |
| 独立窗口 | `display: standalone`，无浏览器地址栏 |
| 竖屏优先 | `orientation: portrait` |
| 系统通知 | 通过 Service Worker 发送提醒通知 |

### 7.14 辅助功能

#### 7.14.1 示例数据

一键生成演示数据，方便新用户快速了解应用功能。

#### 7.14.2 清空数据

清除所有本地数据，需二次确认操作。

## 8. 关键流程

### 8.1 活动记录流程

1. 用户点击底部导航「记录」按钮
2. 弹出活动录入弹窗
3. 可选择模板快速填充或手动填写
4. 填写开始/结束时间、类型、内容等字段
5. 可选上传图片
6. 点击保存
7. 时间线刷新展示新记录
8. 若开启自动同步，自动上传到云端

### 8.2 健康数据记录流程

1. 用户点击底部导航「健康」按钮
2. 弹出健康数据录入弹窗
3. 选择指标类型，系统自动填充单位
4. 输入数值，系统做范围校验
5. 可选填备注和上传图片
6. 保存后刷新健康摘要和时间线

### 8.3 症状记录流程

1. 用户点击底部导航「症状」按钮
2. 弹出症状记录弹窗
3. 填写症状发生时间和描述
4. 可选填处理措施和上传图片
5. 保存后在时间线中展示

### 8.4 提醒处理流程

1. 用户创建提醒（设置内容、日期、时间、重复规则）
2. 到点后触发系统通知或应用内弹窗
3. 用户可选择：
   - 标记完成
   - 延后提醒（预设或自定义时长）
   - 稍后处理（关闭弹窗）

### 8.5 导入导出流程

1. 用户点击「导出」按钮进入数据管理弹窗
2. 查看存储状态和备份提醒
3. 导出：选择导出范围 → 预览数据量 → 选择 JSON/CSV 格式 → 下载
4. 导入：选择 JSON 文件 → 预览数据量 → 选择合并/替换 → 确认导入

### 8.6 AI 诊断流程

1. 用户通过「一键 AI 诊断」或工具栏入口打开 AI 诊断弹窗
2. 首次使用需配置 AI 服务（提供商、API 密钥等）
3. 测试连接后保存配置
4. 选择分析数据范围和诊断选项
5. 点击「开始 AI 诊断」
6. 系统汇总相关数据、构建 Prompt、调用 AI 接口
7. 展示诊断过程和分析结果
8. 可继续追问，进入多轮对话

### 8.7 云同步流程

1. 用户打开云同步弹窗
2. 输入同步服务地址（或使用默认）
3. 输入账号密码并登录（未注册自动创建）
4. 登录成功后显示同步操作面板
5. 选择「上传到云端」或「从云端恢复」
6. 可开启自动同步

## 9. 数据与存储

### 9.1 本地存储策略

| 存储引擎 | 优先级 | 说明 |
|----------|--------|------|
| IndexedDB | 优先 | 支持更大容量，结构化存储 |
| localStorage | 降级 | IndexedDB 不可用时自动降级 |

应用初始化时自动检测存储引擎可用性，并将旧版 localStorage 数据迁移到 IndexedDB。

### 9.2 数据结构

```
{
  activities: { "2026-03-14": [...], ... },      // 按日期分组的活动数组
  healthRecords: { "2026-03-14": [...], ... },   // 按日期分组的健康数据
  symptomRecords: { "2026-03-14": [...], ... },  // 按日期分组的症状记录
  dailyNotes: { "2026-03-14": "...", ... },      // 按日期的状态记录文本
  reminders: [...],                               // 全量提醒列表
  profile: { name, gender, age, ... },            // 个人信息
  metadata: { schemaVersion, storageEngine, ... } // 元信息
}
```

### 9.3 核心数据对象

- Activity：活动记录
- HealthRecord：健康数据
- SymptomRecord：症状记录
- DailyNote：每日状态记录
- Reminder：提醒
- Profile：个人信息
- Template：活动模板
- AIConfig：AI 服务配置
- Metadata：应用元信息（schema 版本、存储引擎、备份状态）

### 9.4 Schema 版本

- 当前版本：2
- 备份提醒周期：7 天

### 9.5 防重复提交

所有表单提交使用 `submitLocks` 机制，包括：活动、健康、症状、提醒、导入五个锁。

### 9.6 数据安全原则

- 数据默认保存在本地浏览器
- 云同步为可选功能，不强制
- 支持导出备份
- 清空数据需二次确认
- API 密钥仅存本地

## 10. 后端 API

### 10.1 技术栈

- .NET 8 Minimal API
- MySQL（腾讯云）
- JWT Bearer 认证
- CORS 全开放

### 10.2 API 端点

#### 服务状态

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | / | 服务信息 |
| GET | /health | 健康检查 |

#### 认证

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/register | 注册（账号+密码+昵称） |
| POST | /api/auth/login | 登录（账号+密码） |
| GET | /api/auth/me | 获取当前用户信息（需认证） |

#### 活动记录

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/activities?date=YYYY-MM-DD | 获取指定日期的活动 |
| POST | /api/activities | 创建活动 |
| PUT | /api/activities/:id | 更新活动 |
| DELETE | /api/activities/:id | 软删除活动 |

#### 健康数据

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/health-records?date=YYYY-MM-DD | 获取指定日期的健康数据 |
| POST | /api/health-records | 创建健康数据 |
| PUT | /api/health-records/:id | 更新健康数据 |
| DELETE | /api/health-records/:id | 软删除健康数据 |

#### 症状记录

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/symptom-records?date=YYYY-MM-DD | 获取指定日期的症状 |
| POST | /api/symptom-records | 创建症状记录 |
| PUT | /api/symptom-records/:id | 更新症状记录 |
| DELETE | /api/symptom-records/:id | 软删除症状记录 |

#### 提醒

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/reminders | 获取所有提醒 |
| GET | /api/reminders/today | 获取今日提醒 |
| GET | /api/reminders/upcoming | 获取即将到来的提醒 |
| POST | /api/reminders | 创建提醒 |
| PUT | /api/reminders/:id | 更新提醒 |
| DELETE | /api/reminders/:id | 软删除提醒 |
| POST | /api/reminders/:id/complete | 标记提醒为完成 |
| POST | /api/reminders/:id/reopen | 重新打开已完成的提醒 |
| POST | /api/reminders/:id/snooze | 延后提醒（需传 minutes 参数） |

#### 个人信息

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/profile | 获取个人信息 |
| PUT | /api/profile | 更新个人信息 |

#### 每日状态记录

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/daily-notes/:date | 获取指定日期的状态记录 |
| PUT | /api/daily-notes/:date | 创建/更新状态记录 |
| DELETE | /api/daily-notes/:date | 软删除状态记录 |

#### 快照（旧版全量同步）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /snapshot | 获取全量快照 |
| PUT | /snapshot | 保存全量快照 |

### 10.3 后端数据校验

| 接口 | 必填校验 |
|------|----------|
| 活动 | activityDate, type, content |
| 健康数据 | recordDate, type, value |
| 症状 | recordDate, description |
| 提醒 | title, type, reminderDate, reminderTime |
| 每日状态 | noteDate, content |

### 10.4 删除策略

所有删除操作为软删除（Soft Delete），数据不实际销毁。

## 11. 响应式设计

### 11.1 断点

| 断点 | 说明 |
|------|------|
| > 768px | 桌面端：宽布局，弹窗居中 |
| 601-768px | 平板端：过渡布局，Modal 宽度调整 |
| ≤ 600px | 手机端：全宽底部导航栏 + 汉堡菜单 + 底部 Sheet 弹窗 |
| ≤ 480px | 小屏手机：进一步缩小间距和字体 |

### 11.2 桌面端 vs 移动端差异

| 组件 | 桌面端（>600px） | 移动端（≤600px） |
|------|------------------|------------------|
| Header 工具栏 | 所有按钮直接显示 | 仅显示「导出」+ 「⋮」汉堡菜单 |
| 底部操作 | 右下角 FAB 圆形浮动按钮组 | 全宽底部 TabBar（图标+文字标签） |
| Modal 弹窗 | 居中弹窗 | 底部 Sheet 上滑动画，顶部有拖拽手柄 |
| 底部导航高度 | 无 | 64px（`--nav-height`） |

## 12. 非功能需求

### 12.1 性能

- 首屏加载应在 2 秒内完成（PWA 缓存后）
- 时间线渲染应流畅，单日 50 条记录无卡顿
- 图表与长列表在常规数据量下保持流畅
- 图片以 Base64 存储，上传前应压缩

### 12.2 安全

- API 密钥仅存储在本地浏览器，不传输到自有服务器
- 用户密码在后端使用哈希存储
- JWT Token 有过期时间，Clock Skew ≤ 1 分钟
- 对用户输入做转义处理，防止 XSS
- 导入文件做格式校验
- 图片类型与大小做约束

### 12.3 可用性

- 移动端操作优先，主要功能 1-2 步可达
- 支持离线使用（PWA + 本地存储）
- 网络恢复后可手动/自动同步到云端
- 备份提醒（默认 7 天未备份时提醒）
- 核心操作要有明确反馈

### 12.4 兼容性

- 移动端浏览器：Chrome、Safari（iOS）、微信内置浏览器
- 桌面端浏览器：Chrome、Edge、Firefox
- 支持添加到手机主屏幕（PWA）
- 在不支持部分能力（IndexedDB、通知权限等）时允许降级

## 13. 验收基线

当前版本至少应满足以下验收要求：

- [ ] 用户可新增、编辑、删除活动并在时间线看到结果
- [ ] 用户可通过日历面板跳转到任意日期
- [ ] 用户可记录健康数据并在首页摘要区看到最新值
- [ ] 用户可记录症状并在时间线看到红色症状卡片
- [ ] 用户可填写今日状态记录并保存
- [ ] 用户可新增、编辑、完成、删除和延后提醒
- [ ] 用户可收到到点提醒弹窗通知
- [ ] 用户可查看周/月统计及趋势图表
- [ ] 用户可导出当前日期、日期范围和全量数据（JSON/CSV）
- [ ] 用户可导入 JSON 并选择合并或替换
- [ ] 用户可使用模板快速创建活动
- [ ] 用户可填写个人档案并长期保留
- [ ] 用户可配置 AI 服务并生成健康诊断报告
- [ ] 用户可在 AI 诊断后继续追问
- [ ] 用户可注册/登录账号并进行云同步
- [ ] 用户可开启自动同步
- [ ] 用户可在支持环境下离线访问应用
- [ ] 用户可在移动端获得完整功能体验（底部导航 + Bottom Sheet）

## 14. 当前版本已知限制

- 前端脚本集中在单文件（app.js ~5000 行）中，维护成本高
- 图片以 Data URL 存储，空间占用偏大
- 提醒能力依赖浏览器通知与前台运行条件
- AI 能力依赖用户自行配置外部 API 服务
- 云同步为全量快照 + 增量 API 并行，缺少冲突合并策略

## 15. 后续版本方向

### 15.1 P1（高优先级）

- 模块化重构（拆分 app.js）
- 测试体系建设
- 云同步冲突解决策略
- 健康数据趋势图表（血压、血糖曲线）

### 15.2 P2（中优先级）

- PDF 健康报告导出
- 标签与分类体系
- 批量操作（批量删除、批量导出）
- 更丰富的统计分析（活动类型分布、健康趋势）

### 15.3 P3（低优先级）

- 第三方设备数据接入（蓝牙血压计等）
- 更完整的健康异常识别辅助
- 多语言支持
- 家庭成员管理

## 16. 文档说明

本 PRD 以当前仓库实现为基线（V4.0，2026-03-14），涵盖前端全部功能模块和已实现的后端 API。用于后续产品收口、模块重构和版本规划。
