# 产品需求文档（PRD）

## 1. 文档信息

- 产品名称：健康生活记录器 / 日常活动记录器
- 文档版本：V1.0
- 文档日期：2026-03-09
- 产品形态：移动优先的单页 Web 应用（静态站点）
- 当前实现状态：已上线基础可用版本

## 2. 产品概述

### 2.1 产品背景

用户在日常健康管理中，常常需要记录饮食、用药、运动和身体感受，同时希望通过简单提醒维持规律作息。现有通用笔记工具记录成本高、结构不统一，健康类 App 又常伴随登录、云同步、广告或复杂流程，不适合轻量自用。

本产品定位为一个轻量、免登录、可本地保存的健康生活记录工具，帮助用户以最低成本记录每日活动并查看当日健康行为概览。

### 2.2 产品目标

- 帮助用户快速记录日常健康相关活动
- 帮助用户形成规律的饮食、用药、运动习惯
- 提供按日查看和基础统计能力，便于回顾
- 提供提醒与导出能力，支持持续使用和数据备份

### 2.3 产品定位

一个面向个人用户的轻量健康记录工具，强调：

- 无需注册登录
- 数据本地保存
- 打开即用
- 以手机端使用体验为主

## 3. 目标用户

### 3.1 用户类型

- 需要记录饮食、用药、运动的个人用户
- 有基础健康管理需求的中老年用户
- 需要观察身体反应与生活习惯关系的用户
- 需要低门槛提醒功能的用户

### 3.2 用户特征

- 更关注简单易用，而非复杂功能
- 更关注隐私，不希望上传敏感健康数据
- 使用设备以手机浏览器为主，也可能在电脑端访问
- 能接受手动导出备份，不强依赖实时同步

## 4. 核心使用场景

### 4.1 记录用餐

用户在早餐、午餐、晚餐或加餐后，记录时间、内容和感受，用于回顾饮食规律和身体反应。

### 4.2 记录用药

用户在服药后，记录服药时间、药物名称和服药后的身体反应，用于减少漏服并积累个人健康数据。

### 4.3 记录运动

用户在运动后，记录运动内容、时间、时长和感受，用于查看当天运动次数和总时长。

### 4.4 记录睡眠

用户可记录入睡、午休、补觉等睡眠相关活动，用于回顾睡眠时间与身体状态变化。

### 4.5 设置提醒

用户提前创建吃饭、吃药、运动等提醒，在指定时间收到浏览器通知或应用内提醒。

### 4.6 查看当天概况

用户打开首页即可看到当日用餐次数、用药次数、运动次数和运动时长，并浏览时间线。

### 4.7 导出数据备份

用户定期将记录导出为 JSON 或 CSV，用于本地备份或使用 Excel 分析。

## 5. 产品范围

### 5.1 本期范围

- 活动记录
- 活动编辑与删除
- 按天浏览历史记录
- 当日统计展示
- 提醒创建、查看、完成、删除
- 每日重复提醒
- JSON / CSV 导出
- 本地存储
- 移动端适配

### 5.2 不在本期范围

- 用户注册登录
- 云端同步
- 多设备实时同步
- 服务端存储
- 提醒编辑
- 数据导入
- 图表分析
- 医疗诊断建议
- 照片上传

## 6. 信息架构

### 6.1 页面结构

当前产品为单页面结构，主要由以下区域构成：

1. 顶部日期区
2. 当日统计卡片区
3. 今日提醒区
4. 活动时间线区
5. 底部快捷操作区
6. 活动录入弹窗
7. 提醒管理弹窗
8. 导出弹窗

### 6.2 导航方式

- 通过顶部左右按钮切换日期
- 通过底部悬浮按钮进入新增活动与提醒管理
- 通过弹窗内标签切换不同提醒视图

## 7. 功能需求

### 7.1 活动记录模块

#### 7.1.1 新增活动

用户点击底部“+”按钮后，打开活动录入弹窗，填写以下字段：

- 时间：必填，精确到分钟
- 类型：必填
- 内容：必填
- 感受：选填
- 时长：选填，单位为分钟

活动类型包括：

- 用餐
- 用药
- 运动
- 睡眠
- 工作
- 其他

#### 7.1.2 编辑活动

用户可对已有活动进行编辑，修改原有字段并保存。

#### 7.1.3 删除活动

用户可删除已有活动，删除前需进行二次确认。

#### 7.1.4 时间线展示

系统按时间顺序展示当前日期下的全部活动记录，每条记录显示：

- 时间
- 类型图标
- 内容
- 感受
- 时长
- 编辑 / 删除操作

#### 7.1.5 空状态

当某日无活动记录时，页面应展示空状态提示，引导用户新增记录。

### 7.2 日期浏览模块

#### 7.2.1 按日切换

用户可通过顶部左右按钮切换前一天或后一天，查看对应日期的活动记录。

#### 7.2.2 日期展示

页面顶部显示当前所查看的完整日期信息，包括年、月、日和星期。

### 7.3 统计模块

系统需基于当前日期下的活动记录，自动统计并展示以下指标：

- 用餐次数
- 用药次数
- 运动次数
- 运动总时长

统计结果需在用户新增、编辑、删除活动后实时更新。

### 7.4 提醒模块

#### 7.4.1 新增提醒

用户可新增提醒，字段包括：

- 提醒内容：必填
- 类型：必填
- 日期：必填
- 时间：必填
- 是否每日重复：选填
- 备注：选填

提醒类型包括：

- 用餐
- 用药
- 运动
- 睡眠
- 其他

#### 7.4.2 提醒视图

提醒管理弹窗内应提供以下标签页：

1. 今日提醒
2. 即将到来
3. 全部提醒
4. 添加提醒

各视图说明：

- 今日提醒：显示当天未完成提醒
- 即将到来：显示当前时间之后最近 10 条未完成提醒，按时间升序排序
- 全部提醒：显示所有提醒，包括已完成和已过期
- 添加提醒：用于录入新提醒

#### 7.4.3 首页提醒摘要

首页展示“今日提醒”摘要区，显示当天最多 3 条未完成提醒。

#### 7.4.4 完成提醒

用户可将提醒标记为完成。

若提醒为每日重复提醒，则在用户完成当前提醒后，系统自动创建下一日同时间的新提醒。

#### 7.4.5 删除提醒

用户可删除提醒，删除前需进行二次确认。

#### 7.4.6 通知触发

当系统检测到当前时间与提醒时间一致时：

- 若用户已授权浏览器通知权限，则触发系统通知
- 若未授权，则以应用内 Toast 方式提示

#### 7.4.7 提醒限制

当前版本不支持编辑提醒。

### 7.5 数据导出模块

#### 7.5.1 导出格式

系统支持以下导出格式：

- JSON：适合完整备份
- CSV：适合在表格工具中查看与分析

#### 7.5.2 导出入口

系统应提供明确可见的导出入口，用户可通过首页按钮、菜单入口或等效显式操作打开导出弹窗。

导出入口需满足以下要求：

- 用户无需依赖隐藏手势或非显式交互
- 用户可在 2 步内进入导出选择界面
- 导出弹窗内提供 JSON 与 CSV 两种导出按钮

#### 7.5.3 导出内容

当前实现为按当前所选日期导出该日活动记录，不包含全部历史数据和提醒数据。

导出文件需包含当前日期标识与当日活动数据。

#### 7.5.4 导出目标

用户导出后，浏览器自动下载文件到本地设备。

### 7.6 本地存储模块

#### 7.6.1 存储方式

所有数据保存在浏览器 LocalStorage 中：

- 活动记录按日期分桶存储
- 提醒记录以数组形式存储

#### 7.6.2 存储原则

- 不依赖后端
- 不上传用户数据
- 不要求用户登录

#### 7.6.3 风险提示

若用户清除浏览器缓存、更换浏览器或更换设备，数据可能丢失。

## 8. 交互与体验要求

### 8.1 设计原则

- 简洁清晰
- 移动优先
- 操作路径短
- 学习成本低

### 8.2 关键交互要求

- 主要操作按钮固定在页面底部，便于手机单手操作
- 弹窗支持点击关闭按钮、取消按钮、遮罩区关闭
- 首页提醒摘要支持点击后进入提醒管理弹窗
- 支持快捷键：
  - `Ctrl + N`：新增活动
  - `Esc`：关闭弹窗
- 用户提交后应有 Toast 反馈

### 8.3 响应式要求

- 页面需适配手机端与桌面端
- 核心信息优先在 360px 以上宽度正常展示

## 9. 非功能需求

### 9.1 性能要求

- 首屏快速加载，无需构建与接口请求
- 本地数据读写应在用户体感上接近实时

### 9.2 可用性要求

- 首次打开即可使用
- 新增活动流程应控制在 3 步以内完成
- 进入提醒管理流程应控制在 1 步内完成
- 进入导出选择界面应控制在 2 步内完成
- 主要功能无需登录即可访问

### 9.3 兼容性要求

- 支持现代移动浏览器
- 支持现代桌面浏览器
- 通知能力依赖浏览器是否支持 Notification API

### 9.4 隐私与安全要求

- 不采集用户账号信息
- 不上传健康记录到服务器
- 不依赖第三方登录

## 10. 已知限制

以下内容属于当前版本已知限制，应在产品说明中明确告知用户：

1. 提醒功能依赖页面处于运行状态，关闭浏览器后无法保证触发提醒
2. 当前仅声明了 PWA 能力，但离线缓存能力未完整实现
3. 导出能力当前仅覆盖当前所选日期的活动数据，不覆盖提醒数据与全量历史数据
4. 当前版本不支持提醒编辑
5. 当前版本不支持数据导入
6. 当前版本不支持多设备同步

## 11. 用户故事

1. 作为一名需要规律吃药的用户，我希望设置每日重复提醒，以便我不漏服药物。
2. 作为一名关注饮食健康的用户，我希望记录每次用餐内容和感受，以便我回顾哪些食物更适合自己。
3. 作为一名坚持运动的用户，我希望记录运动次数和时长，以便我查看每天是否达成目标。
4. 作为一名重视隐私的用户，我希望数据仅保存在本地浏览器中，以便避免上传个人健康信息。
5. 作为一名需要备份数据的用户，我希望能导出 JSON 或 CSV，以便后续保存或分析。

## 12. 验收标准

### 12.1 活动记录验收

- 用户可以新增活动并立即在时间线看到结果
- 用户可以编辑活动且修改后内容正确更新
- 用户可以删除活动且删除后统计同步更新
- 空白日期能展示空状态

### 12.2 提醒功能验收

- 用户可以新增提醒并在对应提醒列表中看到
- 今日提醒和首页提醒摘要展示一致
- 即将到来列表仅展示最近 10 条未完成提醒，且按时间升序排列
- 用户可以标记提醒完成
- 每日重复提醒在完成后能自动生成下一日提醒
- 用户可以删除提醒

### 12.3 导出功能验收

- 页面存在明确可见的导出入口，用户可进入导出弹窗
- 用户触发导出后，浏览器可下载 JSON 文件
- 用户触发导出后，浏览器可下载 CSV 文件
- 导出内容仅包含当前所选日期的活动数据
- CSV 文件可在 Excel 中正常打开

### 12.4 存储验收

- 刷新页面后，活动数据和提醒数据仍可保留
- 切换日期后，可看到对应日期的历史记录

## 13. 后续版本规划建议

### 13.1 V1.1 建议

- 补齐导出入口的显式 UI
- 支持提醒编辑
- 修正文档与实际功能不一致问题

### 13.2 V1.2 建议

- 增加数据导入能力
- 增加图表统计
- 增加提醒筛选与搜索

### 13.3 V2.0 建议

- 增加账号体系与云同步
- 增加多设备共享
- 增加更完整的健康指标记录，如体重、血压、血糖

## 14. 附录

### 14.1 当前技术方案

- 前端：HTML + CSS + 原生 JavaScript
- 存储：LocalStorage
- 部署：Vercel / GitHub Pages / 任意静态托管平台

### 14.2 相关文档

- `README.md`
- `HOW_TO_USE.md`
- `HEALTH_GUIDE.md`
- `REMINDER_GUIDE.md`
- `VERCEL_DEPLOY.md`
- `GITHUB_DEPLOY.md`
